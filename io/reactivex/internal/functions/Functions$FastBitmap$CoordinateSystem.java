package io.reactivex.internal.functions;

import o.getChildren;
import o.showRedPoint;

final class Functions$FastBitmap$CoordinateSystem<T> implements getChildren<T> {
    final showRedPoint length;

    Functions$FastBitmap$CoordinateSystem(showRedPoint showredpoint) {
        this.length = showredpoint;
    }

    public final boolean test(T t) throws Exception {
        return !this.length.setMin();
    }
}
