package io.reactivex.internal.functions;

import o.getChildren;
import o.setRefreshAnimation;

final class Functions$Mean$Arithmetic<T> implements getChildren<T> {
    final T setMax;

    Functions$Mean$Arithmetic(T t) {
        this.setMax = t;
    }

    public final boolean test(T t) throws Exception {
        return setRefreshAnimation.getMin(t, this.setMax);
    }
}
