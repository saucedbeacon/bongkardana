package androidx.lifecycle;

import androidx.annotation.NonNull;
import androidx.lifecycle.Lifecycle;
import o.needsDividerAfter;
import o.onClick;
import o.setChecked;
import o.setPopupCallback;

public class CompositeGeneratedAdaptersObserver implements setPopupCallback {
    private final onClick[] getMax;

    public CompositeGeneratedAdaptersObserver(onClick[] onclickArr) {
        this.getMax = onclickArr;
    }

    public void setMax(@NonNull setChecked setchecked, @NonNull Lifecycle.Event event) {
        new needsDividerAfter();
    }
}
