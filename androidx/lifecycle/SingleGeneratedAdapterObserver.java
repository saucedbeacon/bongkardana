package androidx.lifecycle;

import androidx.annotation.NonNull;
import androidx.lifecycle.Lifecycle;
import o.onClick;
import o.setChecked;
import o.setPopupCallback;

public class SingleGeneratedAdapterObserver implements setPopupCallback {
    private final onClick setMin;

    public void setMax(@NonNull setChecked setchecked, @NonNull Lifecycle.Event event) {
    }

    public SingleGeneratedAdapterObserver(onClick onclick) {
        this.setMin = onclick;
    }
}
