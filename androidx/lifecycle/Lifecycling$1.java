package androidx.lifecycle;

import androidx.annotation.NonNull;
import androidx.lifecycle.Lifecycle;
import o.setChecked;
import o.setItemInvoker;
import o.setPopupCallback;

public class Lifecycling$1 implements setItemInvoker {
    final /* synthetic */ setPopupCallback setMax;

    public void setMax(@NonNull setChecked setchecked, @NonNull Lifecycle.Event event) {
        this.setMax.setMax(setchecked, event);
    }
}
