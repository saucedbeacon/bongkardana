package androidx.viewpager2.adapter;

import androidx.annotation.NonNull;
import androidx.lifecycle.Lifecycle;
import o.setChecked;
import o.setPopupCallback;
import o.showPopupUnchecked;

public class FragmentStateAdapter$FragmentMaxLifecycleEnforcer$3 implements setPopupCallback {
    final /* synthetic */ showPopupUnchecked.setMin length;

    public FragmentStateAdapter$FragmentMaxLifecycleEnforcer$3(showPopupUnchecked.setMin setmin) {
        this.length = setmin;
    }

    public void setMax(@NonNull setChecked setchecked, @NonNull Lifecycle.Event event) {
        this.length.getMax(false);
    }
}
