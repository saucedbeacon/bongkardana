package androidx.work.impl.diagnostics;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.work.impl.workers.DiagnosticsWorker;
import java.util.Collections;
import o.setBackgroundDrawable;
import o.setSupportBackgroundTintList;
import o.setTextSize;

public class DiagnosticsReceiver extends BroadcastReceiver {
    private static final String getMin = setSupportBackgroundTintList.length("DiagnosticsRcvr");

    public void onReceive(@NonNull Context context, @Nullable Intent intent) {
        if (intent != null) {
            setSupportBackgroundTintList.getMax();
            try {
                setTextSize.getMax(context).length(Collections.singletonList((setBackgroundDrawable) new setBackgroundDrawable.getMin(DiagnosticsWorker.class).setMin()));
            } catch (IllegalStateException e) {
                setSupportBackgroundTintList.getMax();
                new Throwable[1][0] = e;
            }
        }
    }
}
