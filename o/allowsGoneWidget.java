package o;

import android.graphics.Bitmap;
import androidx.annotation.NonNull;
import java.security.MessageDigest;

public final class allowsGoneWidget extends setType {
    private static final byte[] getMin = "com.bumptech.glide.load.resource.bitmap.CenterCrop".getBytes(setMin);

    /* access modifiers changed from: protected */
    public final Bitmap setMax(@NonNull rebuildScene rebuildscene, @NonNull Bitmap bitmap, int i, int i2) {
        return setTag.getMax(rebuildscene, bitmap, i, i2);
    }

    public final boolean equals(Object obj) {
        return obj instanceof allowsGoneWidget;
    }

    public final int hashCode() {
        return "com.bumptech.glide.load.resource.bitmap.CenterCrop".hashCode();
    }

    public final void setMax(@NonNull MessageDigest messageDigest) {
        messageDigest.update(getMin);
    }
}
