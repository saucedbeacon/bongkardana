package o;

import o.addAnimatorUpdateListener;

final class onStateChanged implements addAnimatorUpdateListener.getMax {
    private final onSocketOpen setMax;

    public onStateChanged(onSocketOpen onsocketopen) {
        this.setMax = onsocketopen;
    }

    public final Object processFacade(Object obj) {
        return ((onSocketMessage) obj).closePayers(this.setMax);
    }
}
