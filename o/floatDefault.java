package o;

import o.addAnimatorUpdateListener;

final class floatDefault implements addAnimatorUpdateListener.getMax {
    private final isFromRemote setMax;

    public floatDefault(isFromRemote isfromremote) {
        this.setMax = isfromremote;
    }

    public final Object processFacade(Object obj) {
        return ((BindingCallback) obj).getNearbyMeShops(this.setMax);
    }
}
