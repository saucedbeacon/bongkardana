package o;

final class sendUserNotGrantPermission implements RedDotDrawable {
    private final postOnWorker getMin;

    public sendUserNotGrantPermission(postOnWorker postonworker) {
        this.getMin = postonworker;
    }

    public final Object apply(Object obj) {
        return this.getMin.apply(obj);
    }
}
