package o;

import android.graphics.PointF;
import com.airbnb.lottie.parser.moshi.JsonReader;
import java.io.IOException;
import o.SearchView;

final class getTrackTintMode {
    private static JsonReader.setMax getMax = JsonReader.setMax.getMin("nm", "p", "s", "hd", "d");

    static SearchView.SearchAutoComplete getMax(JsonReader jsonReader, setVerticalGravity setverticalgravity, int i) throws IOException {
        boolean z = i == 3;
        String str = null;
        SearchView.SavedState<PointF, PointF> savedState = null;
        onSearchClicked onsearchclicked = null;
        boolean z2 = false;
        while (jsonReader.getMin()) {
            int min = jsonReader.setMin(getMax);
            if (min == 0) {
                str = jsonReader.toIntRange();
            } else if (min == 1) {
                savedState = getTrackTintList.setMax(jsonReader, setverticalgravity);
            } else if (min == 2) {
                onsearchclicked = new onSearchClicked(getSplitTrack.setMin(jsonReader, setverticalgravity, setTitleMarginStart.getMax(), makeLayout.length, true));
            } else if (min == 3) {
                z2 = jsonReader.equals();
            } else if (min != 4) {
                jsonReader.isInside();
                jsonReader.FastBitmap$CoordinateSystem();
            } else {
                z = jsonReader.values() == 3;
            }
        }
        return new SearchView.SearchAutoComplete(str, savedState, onsearchclicked, z, z2);
    }
}
