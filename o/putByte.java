package o;

import java.util.List;
import o.addAnimatorUpdateListener;

final class putByte implements addAnimatorUpdateListener.getMax {
    private final putFloat getMin;
    private final List length;

    public putByte(List list, putFloat putfloat) {
        this.length = list;
        this.getMin = putfloat;
    }

    public final Object processFacade(Object obj) {
        return ((putBoolean) obj).queryMissionListByStatus(new getFloat(this.length, this.getMin));
    }
}
