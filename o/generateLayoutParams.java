package o;

import android.view.View;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import o.killMode;

public final class generateLayoutParams {
    public static void setMin(@NonNull View view, @Nullable onInitializeAccessibilityEvent oninitializeaccessibilityevent) {
        view.setTag(killMode.setMax.length, oninitializeaccessibilityevent);
    }
}
