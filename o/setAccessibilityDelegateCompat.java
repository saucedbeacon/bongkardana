package o;

import android.content.Context;
import android.content.res.TypedArray;
import androidx.annotation.NonNull;
import com.otaliastudios.cameraview.controls.Audio;
import com.otaliastudios.cameraview.controls.Engine;
import com.otaliastudios.cameraview.controls.Facing;
import com.otaliastudios.cameraview.controls.Flash;
import com.otaliastudios.cameraview.controls.Grid;
import com.otaliastudios.cameraview.controls.Hdr;
import com.otaliastudios.cameraview.controls.Mode;
import com.otaliastudios.cameraview.controls.PictureFormat;
import com.otaliastudios.cameraview.controls.Preview;
import com.otaliastudios.cameraview.controls.VideoCodec;
import com.otaliastudios.cameraview.controls.WhiteBalance;
import o.calculateDyToMakeVisible;

public final class setAccessibilityDelegateCompat {
    public int IsOverlapping;
    public int equals;
    public int getMax;
    public int getMin;
    public int isInside;
    public int length;
    public int setMax;
    public int setMin;
    public int toFloatRange;
    public int toIntRange;
    public int values;

    public setAccessibilityDelegateCompat(@NonNull Context context, @NonNull TypedArray typedArray) {
        this.getMin = typedArray.getInteger(calculateDyToMakeVisible.setMax.getInterfaceDescriptor, Preview.DEFAULT.value());
        this.setMin = typedArray.getInteger(calculateDyToMakeVisible.setMax.toIntRange, Facing.DEFAULT(context).value());
        this.setMax = typedArray.getInteger(calculateDyToMakeVisible.setMax.equals, Flash.DEFAULT.value());
        this.getMax = typedArray.getInteger(calculateDyToMakeVisible.setMax.onNavigationEvent, Grid.DEFAULT.value());
        this.length = typedArray.getInteger(calculateDyToMakeVisible.setMax.IPostMessageService$Stub$Proxy, WhiteBalance.DEFAULT.value());
        this.IsOverlapping = typedArray.getInteger(calculateDyToMakeVisible.setMax.create, Mode.DEFAULT.value());
        this.isInside = typedArray.getInteger(calculateDyToMakeVisible.setMax.getCause, Hdr.DEFAULT.value());
        this.toIntRange = typedArray.getInteger(calculateDyToMakeVisible.setMax.setMin, Audio.DEFAULT.value());
        this.toFloatRange = typedArray.getInteger(calculateDyToMakeVisible.setMax.newSessionWithExtras, VideoCodec.DEFAULT.value());
        this.equals = typedArray.getInteger(calculateDyToMakeVisible.setMax.isInside, Engine.DEFAULT.value());
        this.values = typedArray.getInteger(calculateDyToMakeVisible.setMax.ICustomTabsCallback, PictureFormat.DEFAULT.value());
    }
}
