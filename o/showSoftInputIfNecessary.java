package o;

import com.airbnb.lottie.LottieDrawable;
import java.util.Arrays;
import java.util.List;

public final class showSoftInputIfNecessary implements isLandscapeMode {
    private final boolean getMax;
    public final List<isLandscapeMode> getMin;
    public final String length;

    public showSoftInputIfNecessary(String str, List<isLandscapeMode> list, boolean z) {
        this.length = str;
        this.getMin = list;
        this.getMax = z;
    }

    public final boolean setMin() {
        return this.getMax;
    }

    public final animateToTab length(LottieDrawable lottieDrawable, onKeyPreIme onkeypreime) {
        return new createTabView(lottieDrawable, onkeypreime, this);
    }

    public final String toString() {
        StringBuilder sb = new StringBuilder("ShapeGroup{name='");
        sb.append(this.length);
        sb.append("' Shapes: ");
        sb.append(Arrays.toString(this.getMin.toArray()));
        sb.append('}');
        return sb.toString();
    }
}
