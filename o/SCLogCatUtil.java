package o;

public final class SCLogCatUtil extends onUserConfirm {
    createJavaLogImpl getMax = new createJavaLogImpl();
    private final suspendEncoding setMin = new suspendEncoding();

    public final void I_() {
    }

    public final SCNetworkAsyncTaskExecutorFactory getMax() {
        return this.setMin;
    }

    public final setRsaPublicKey setMax(setEncryptRandomStringAndType setencryptrandomstringandtype) {
        if (!setencryptrandomstringandtype.toIntRange()) {
            return new isAndroidPlatform(setencryptrandomstringandtype.setMin(), -1, false);
        }
        return null;
    }

    public final void getMax(CharSequence charSequence) {
        this.getMax.setMin(charSequence);
    }

    public final void setMax(MonitorPrinter monitorPrinter) {
        int length;
        String obj = this.getMax.getMax.toString();
        boolean z = false;
        while (obj.length() > 3 && obj.charAt(0) == '[' && (length = monitorPrinter.length(obj)) != 0) {
            obj = obj.substring(length);
            z = true;
        }
        if (!z || !SSLExtensions.length(obj)) {
            this.getMax = new createJavaLogImpl(obj);
            return;
        }
        this.setMin.toIntRange();
        this.getMax = null;
    }

    public final void getMax(asciiBytes asciibytes) {
        createJavaLogImpl createjavalogimpl = this.getMax;
        if (createjavalogimpl != null) {
            asciibytes.getMax(createjavalogimpl.getMax.toString(), this.setMin);
        }
    }
}
