package o;

import android.content.Context;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import java.util.ArrayList;
import java.util.List;
import o.setImageDrawable;

public final class getTextClassifier implements setImageDrawable.getMax {
    private static final String setMax = setSupportBackgroundTintList.length("WorkConstraintsTracker");
    @Nullable
    private final onDragEvent getMax;
    private final setImageDrawable<?>[] length;
    private final Object setMin = new Object();

    public getTextClassifier(@NonNull Context context, @NonNull getMinWidthMinor getminwidthminor, @Nullable onDragEvent ondragevent) {
        Context applicationContext = context.getApplicationContext();
        this.getMax = ondragevent;
        this.length = new setImageDrawable[]{new getText(applicationContext, getminwidthminor), new AppCompatImageButton(applicationContext, getminwidthminor), new setSupportImageTintMode(applicationContext, getminwidthminor), new setImageResource(applicationContext, getminwidthminor), new getSupportImageTintList(applicationContext, getminwidthminor), new onReceiveContent(applicationContext, getminwidthminor), new setImageBitmap(applicationContext, getminwidthminor)};
    }

    public final void getMax(@NonNull Iterable<getInternalPopup> iterable) {
        synchronized (this.setMin) {
            for (setImageDrawable<?> setimagedrawable : this.length) {
                if (setimagedrawable.getMin != null) {
                    setimagedrawable.getMin = null;
                    setimagedrawable.setMax(setimagedrawable.getMin, setimagedrawable.setMax);
                }
            }
            for (setImageDrawable<?> min : this.length) {
                min.getMin(iterable);
            }
            for (setImageDrawable<?> setimagedrawable2 : this.length) {
                if (setimagedrawable2.getMin != this) {
                    setimagedrawable2.getMin = this;
                    setimagedrawable2.setMax(setimagedrawable2.getMin, setimagedrawable2.setMax);
                }
            }
        }
    }

    public final void length() {
        synchronized (this.setMin) {
            for (setImageDrawable<?> setimagedrawable : this.length) {
                if (!setimagedrawable.getMax.isEmpty()) {
                    setimagedrawable.getMax.clear();
                    setimagedrawable.length.length(setimagedrawable);
                }
            }
        }
    }

    public final boolean setMin(@NonNull String str) {
        synchronized (this.setMin) {
            for (setImageDrawable<?> setimagedrawable : this.length) {
                if (setimagedrawable.setMax(str)) {
                    setSupportBackgroundTintList.getMax();
                    setimagedrawable.getClass().getSimpleName();
                    return false;
                }
            }
            return true;
        }
    }

    public final void length(@NonNull List<String> list) {
        synchronized (this.setMin) {
            ArrayList arrayList = new ArrayList();
            for (String next : list) {
                if (setMin(next)) {
                    setSupportBackgroundTintList.getMax();
                    arrayList.add(next);
                }
            }
            if (this.getMax != null) {
                this.getMax.getMin(arrayList);
            }
        }
    }

    public final void getMin(@NonNull List<String> list) {
        synchronized (this.setMin) {
            if (this.getMax != null) {
                this.getMax.getMax(list);
            }
        }
    }
}
