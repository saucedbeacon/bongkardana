package o;

import java.util.Collections;

final class onHoverEvent implements Runnable {
    private final getContentHeight setMin;

    public onHoverEvent(getContentHeight getcontentheight) {
        this.setMin = getcontentheight;
    }

    public final void run() {
        Collections.emptyList();
    }
}
