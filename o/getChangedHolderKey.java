package o;

import android.media.MediaCodec;
import androidx.annotation.RequiresApi;
import o.ensureRightGlow;

@RequiresApi(18)
final class getChangedHolderKey extends ensureRightGlow<animateChange> {
    getChangedHolderKey(final int i) {
        super(Integer.MAX_VALUE, new ensureRightGlow.getMin<animateChange>() {
            public final /* synthetic */ Object setMax() {
                animateChange animatechange = new animateChange();
                animatechange.setMax = i;
                animatechange.getMax = new MediaCodec.BufferInfo();
                return animatechange;
            }
        });
    }
}
