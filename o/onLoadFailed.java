package o;

import id.dana.sendmoney.model.RecentContactModel;
import java.util.List;
import o.onDelete;

public interface onLoadFailed {

    public interface length {
        void length(List<RecentContactModel> list);
    }

    public interface setMax extends onDelete.getMin {
        void getMax();
    }
}
