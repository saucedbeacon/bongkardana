package o;

import o.addAnimatorUpdateListener;

final class getDesc implements addAnimatorUpdateListener.getMax {
    private final setAppKey length;

    public getDesc(setAppKey setappkey) {
        this.length = setappkey;
    }

    public final Object processFacade(Object obj) {
        return ((getVhost) obj).getMiniProgramUserInfo(this.length);
    }
}
