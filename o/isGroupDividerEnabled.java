package o;

import android.text.TextUtils;
import o.getActionViewStatesKey;

class isGroupDividerEnabled {
    private static final boolean length = getActionViewStatesKey.getMin;

    static class setMax implements getActionViewStatesKey.getMax {
        private int length;
        private int setMax;
        private String setMin;

        setMax(String str, int i, int i2) {
            this.setMin = str;
            this.setMax = i;
            this.length = i2;
        }

        public final boolean equals(Object obj) {
            if (this == obj) {
                return true;
            }
            if (!(obj instanceof setMax)) {
                return false;
            }
            setMax setmax = (setMax) obj;
            return TextUtils.equals(this.setMin, setmax.setMin) && this.setMax == setmax.setMax && this.length == setmax.length;
        }

        public final int hashCode() {
            return setNegativeButtonIcon.setMin(this.setMin, Integer.valueOf(this.setMax), Integer.valueOf(this.length));
        }
    }
}
