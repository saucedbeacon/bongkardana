package o;

import javax.inject.Singleton;

@Singleton
public final class onEnteredHiddenState implements getScheme {
    public final hasCornerMarking getScheduler() {
        return hideProgress.length();
    }
}
