package o;

import o.addAnimatorUpdateListener;

final class sendSimpleRpcJsapi implements addAnimatorUpdateListener.getMax {
    private final getResponse getMin;

    public sendSimpleRpcJsapi(getResponse getresponse) {
        this.getMin = getresponse;
    }

    public final Object processFacade(Object obj) {
        return ((getCode) obj).fetchPublicKey(this.getMin);
    }
}
