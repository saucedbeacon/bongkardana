package o;

import id.dana.data.user.source.network.request.UserInfoRequest;
import o.addAnimatorUpdateListener;

final class setFallbackBaseUrl implements addAnimatorUpdateListener.getMax {
    private final UserInfoRequest getMax;

    public setFallbackBaseUrl(UserInfoRequest userInfoRequest) {
        this.getMax = userInfoRequest;
    }

    public final Object processFacade(Object obj) {
        return ((getVhost) obj).getBalance(this.getMax);
    }
}
