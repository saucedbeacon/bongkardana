package o;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import com.otaliastudios.cameraview.CameraLogger;
import o.calculateDtToFit;
import o.onPointerUp;

public abstract class cancelScroll extends onPointerUp {
    protected static final CameraLogger FastBitmap$CoordinateSystem;
    private static final String setMin;

    static {
        String simpleName = cancelScroll.class.getSimpleName();
        setMin = simpleName;
        FastBitmap$CoordinateSystem = CameraLogger.setMin(simpleName);
    }

    public cancelScroll(@NonNull calculateDtToFit.length length, @Nullable onPointerUp.length length2) {
        super(length, length2);
    }
}
