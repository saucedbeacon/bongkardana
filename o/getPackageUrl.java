package o;

import id.dana.data.user.source.network.request.UserInfoRequest;
import o.addAnimatorUpdateListener;

final class getPackageUrl implements addAnimatorUpdateListener.getMax {
    private final UserInfoRequest setMin;

    public getPackageUrl(UserInfoRequest userInfoRequest) {
        this.setMin = userInfoRequest;
    }

    public final Object processFacade(Object obj) {
        return ((getVhost) obj).getUserInfo(this.setMin);
    }
}
