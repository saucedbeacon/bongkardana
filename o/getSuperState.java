package o;

import java.util.ArrayList;
import javax.annotation.Nullable;

public final class getSuperState extends getAccessibilityFocusedVirtualViewId {
    @Nullable
    private final obtainAccessibilityNodeInfo getMin;
    private final describeContents length;

    public getSuperState(describeContents describecontents, obtainAccessibilityNodeInfo obtainaccessibilitynodeinfo) {
        this.length = describecontents;
        this.getMin = obtainaccessibilitynodeinfo;
    }

    public final void setMin(ArrayList<describeContents> arrayList) {
        arrayList.add(this.length);
    }

    /* access modifiers changed from: protected */
    public final void getMax(dispatchHoverEvent dispatchhoverevent) {
        createEvent createevent = new createEvent(this.getMin);
        getAllNodes getallnodes = new getAllNodes(dispatchhoverevent.getMin(this.length.getMin));
        getAllNodes getallnodes2 = new getAllNodes(this.length.length);
        length(getallnodes, createevent, "initial");
        length(getallnodes2, createevent, TtmlNode.END);
        setMax(createevent, dispatchhoverevent.setMin(this.length.getMin));
    }
}
