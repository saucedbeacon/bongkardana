package o;

import dagger.internal.InjectedFieldSignature;
import id.dana.sendmoney.RecipientActivity;
import o.IWorkManagerImplCallback;
import o.enqueueWorkRequests;
import o.getDuration;
import o.getSelectedIndex;

public final class getSourcePath implements getBindingAdapter<RecipientActivity> {
    @InjectedFieldSignature("id.dana.sendmoney.RecipientActivity.recipientPresenter")
    public static void setMax(RecipientActivity recipientActivity, enqueueWorkRequests.length length) {
        recipientActivity.recipientPresenter = length;
    }

    @InjectedFieldSignature("id.dana.sendmoney.RecipientActivity.billerX2BPresenter")
    public static void setMin(RecipientActivity recipientActivity, IWorkManagerImplCallback.Stub.Proxy.getMin getmin) {
        recipientActivity.billerX2BPresenter = getmin;
    }

    @InjectedFieldSignature("id.dana.sendmoney.RecipientActivity.bottomSheetOnBoardingPresenter")
    public static void getMin(RecipientActivity recipientActivity, getSelectedIndex.length length) {
        recipientActivity.bottomSheetOnBoardingPresenter = length;
    }

    @InjectedFieldSignature("id.dana.sendmoney.RecipientActivity.readDeepLinkPropertiesPresenter")
    public static void setMin(RecipientActivity recipientActivity, getDuration.length length) {
        recipientActivity.readDeepLinkPropertiesPresenter = length;
    }
}
