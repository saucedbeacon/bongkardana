package o;

import com.airbnb.lottie.parser.moshi.JsonReader;
import java.io.IOException;

final class cancelPositionAnimator {
    static JsonReader.setMax setMax = JsonReader.setMax.getMin("nm", "ind", "ks", "hd");

    static ensureImeVisible getMin(JsonReader jsonReader, setVerticalGravity setverticalgravity) throws IOException {
        String str = null;
        onItemClicked onitemclicked = null;
        int i = 0;
        boolean z = false;
        while (jsonReader.getMin()) {
            int min = jsonReader.setMin(setMax);
            if (min == 0) {
                str = jsonReader.toIntRange();
            } else if (min == 1) {
                i = jsonReader.values();
            } else if (min == 2) {
                onitemclicked = new onItemClicked(getSplitTrack.setMin(jsonReader, setverticalgravity, setTitleMarginStart.getMax(), getTargetCheckedState.getMin, false));
            } else if (min != 3) {
                jsonReader.FastBitmap$CoordinateSystem();
            } else {
                z = jsonReader.equals();
            }
        }
        return new ensureImeVisible(str, i, onitemclicked, z);
    }
}
