package o;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import o.setExclusiveItemChecked;

final class findGroupIndex<T> extends setExclusiveItemChecked<T> {
    private final boolean getMax;
    private final Object setMax;
    private final removeItemAtInt<?, T> setMin;

    public final boolean equals() {
        return true;
    }

    /* access modifiers changed from: package-private */
    public final void setMax(int i) {
    }

    /* access modifiers changed from: package-private */
    public final void setMin(@NonNull setExclusiveItemChecked<T> setexclusiveitemchecked, @NonNull setExclusiveItemChecked.length length) {
    }

    public final boolean toIntRange() {
        return true;
    }

    findGroupIndex(@NonNull setExclusiveItemChecked<T> setexclusiveitemchecked) {
        super(new hasVisibleItems(setexclusiveitemchecked.FastBitmap$CoordinateSystem), setexclusiveitemchecked.toIntRange, setexclusiveitemchecked.toFloatRange, (setExclusiveItemChecked.setMax) null, setexclusiveitemchecked.toDoubleRange);
        this.setMin = setexclusiveitemchecked.getMax();
        this.getMax = setexclusiveitemchecked.setMin();
        this.values = setexclusiveitemchecked.values;
        this.setMax = setexclusiveitemchecked.getMin();
    }

    /* access modifiers changed from: package-private */
    public final boolean setMin() {
        return this.getMax;
    }

    @Nullable
    public final Object getMin() {
        return this.setMax;
    }

    @NonNull
    public final removeItemAtInt<?, T> getMax() {
        return this.setMin;
    }
}
