package o;

import o.addAnimatorUpdateListener;

final class findUpdater implements addAnimatorUpdateListener.getMax {
    private final setNeedShowError setMin;

    public findUpdater(setNeedShowError setneedshowerror) {
        this.setMin = setneedshowerror;
    }

    public final Object processFacade(Object obj) {
        return ((UpdateAppException) obj).bizSyncContact(this.setMin);
    }
}
