package o;

import id.dana.data.user.source.network.request.UserInfoRequest;
import o.addAnimatorUpdateListener;

final class getFallbackBaseUrl implements addAnimatorUpdateListener.getMax {
    private final UserInfoRequest getMin;

    public getFallbackBaseUrl(UserInfoRequest userInfoRequest) {
        this.getMin = userInfoRequest;
    }

    public final Object processFacade(Object obj) {
        return ((getVhost) obj).getUserInfo(this.getMin);
    }
}
