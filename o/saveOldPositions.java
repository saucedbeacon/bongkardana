package o;

import androidx.annotation.Nullable;
import o.offsetPositionRecordsForInsert;

public class saveOldPositions implements offsetPositionRecordsForInsert.setMax {
    public findViewHolderForAdapterPosition getMax;
    public getMax getMin;
    public offsetPositionRecordsForMove setMin;

    public interface getMax {
        void onIndicatorUpdated();
    }

    public saveOldPositions(@Nullable getMax getmax) {
        this.getMin = getmax;
        findViewHolderForAdapterPosition findviewholderforadapterposition = new findViewHolderForAdapterPosition();
        this.getMax = findviewholderforadapterposition;
        if (findviewholderforadapterposition.setMax == null) {
            findviewholderforadapterposition.setMax = new onChildDetachedFromWindow();
        }
        this.setMin = new offsetPositionRecordsForMove(findviewholderforadapterposition.setMax, this);
    }

    public final void setMin(@Nullable canReuseUpdatedViewHolder canreuseupdatedviewholder) {
        this.getMax.getMin.length = canreuseupdatedviewholder;
        getMax getmax = this.getMin;
        if (getmax != null) {
            getmax.onIndicatorUpdated();
        }
    }
}
