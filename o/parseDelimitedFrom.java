package o;

import android.text.TextUtils;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import java.util.Map;

public final class parseDelimitedFrom extends getAppIDBytes {
    private final setMin getMax;

    interface setMin {
        @Nullable
        getUploadFile length(@NonNull Map<String, String> map);
    }

    public parseDelimitedFrom(@NonNull setMin setmin) {
        this.getMax = setmin;
    }

    @Nullable
    public final Object getMax(@NonNull appendFlushLog appendflushlog, @NonNull getLogEventType getlogeventtype) {
        String str = getlogeventtype.getMax().get("src");
        if (TextUtils.isEmpty(str)) {
            return null;
        }
        return appendflushlog.isInside.setMax(appendflushlog.setMax, appendflushlog.getMin.setMax(str), appendflushlog.setMin, appendflushlog.toFloatRange, this.getMax.length(getlogeventtype.getMax()), false);
    }
}
