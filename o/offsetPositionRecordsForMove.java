package o;

import androidx.annotation.NonNull;
import o.offsetPositionRecordsForInsert;

public final class offsetPositionRecordsForMove {
    public isAnimating getMax;

    public offsetPositionRecordsForMove(@NonNull onChildDetachedFromWindow onchilddetachedfromwindow, @NonNull offsetPositionRecordsForInsert.setMax setmax) {
        this.getMax = new isAnimating(onchilddetachedfromwindow, setmax);
    }
}
