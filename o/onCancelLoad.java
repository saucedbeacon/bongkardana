package o;

import android.view.View;
import android.widget.ExpandableListView;
import kotlin.text.Typography;

public final class onCancelLoad {
    private static char getMax = '\u0004';
    private static int getMin = 0;
    private static int length = 1;
    private static char[] setMax = {'E', 'm', 'u', 'l', 'a', 't', 'o', 'r', ' ', 'd', 'e', 'c', 'F', 'G', 'H', 'I'};

    public static void getMin(int i, onActivityPreDestroyed onactivitypredestroyed) {
        if (!onactivitypredestroyed.getMin()) {
            try {
                if ((!onactivitypredestroyed.setMax() ? 2 : '3') == 2) {
                    if ((!onactivitypredestroyed.setMin() ? 'S' : 'E') == 'E') {
                        return;
                    }
                    if (!onactivitypredestroyed.getMax()) {
                        int i2 = getMin + 63;
                        length = i2 % 128;
                        if (i2 % 2 == 0) {
                            boolean IsOverlapping = onactivitypredestroyed.IsOverlapping();
                            Object obj = null;
                            super.hashCode();
                            if ((!IsOverlapping ? '%' : Typography.less) == '<') {
                                return;
                            }
                        } else if (onactivitypredestroyed.IsOverlapping()) {
                            return;
                        }
                        try {
                            if ((!onactivitypredestroyed.length()) && !onactivitypredestroyed.toFloatRange()) {
                                System.out.print(i / (((i - 1) * i) % 2));
                                int i3 = getMin + 117;
                                length = i3 % 128;
                                int i4 = i3 % 2;
                            }
                        } catch (Exception e) {
                            throw e;
                        }
                    }
                }
            } catch (Exception e2) {
                throw e2;
            }
        }
    }

    public static void setMax(int i, onActivityPreDestroyed onactivitypredestroyed) {
        boolean z = false;
        if ((onactivitypredestroyed.length() ? '+' : 'M') == '+') {
            int i2 = getMin + 95;
            length = i2 % 128;
            int i3 = i2 % 2;
            setMin(new char[]{1, 2, 3, 0, 5, 6, 7, 4, 9, 10, 9, 6, 11, 8, 6, 9, '}'}, ExpandableListView.getPackedPositionChild(0) + 18, (byte) (25 - View.resolveSize(0, 0))).intern();
        }
        onCanceled oncanceled = (onCanceled) onactivitypredestroyed;
        isReset min = registerOnLoadCanceledListener.setMin();
        if (oncanceled.getMin()) {
            min.setMin(oncanceled.setMin, oncanceled.length);
        }
        if ((oncanceled.setMax() ? 'X' : 'U') != 'U') {
            min.length(oncanceled.setMin, oncanceled.length);
        }
        if (oncanceled.setMin()) {
            min.setMax(oncanceled.setMin, oncanceled.length);
        }
        if (oncanceled.length()) {
            min.getMin(oncanceled.setMin, oncanceled.length);
        }
        if (!(!oncanceled.getMax())) {
            min.setMax(oncanceled.setMin, oncanceled.length);
            int i4 = getMin + 13;
            length = i4 % 128;
            int i5 = i4 % 2;
        }
        if (oncanceled.toFloatRange()) {
            min.IsOverlapping(oncanceled.setMin, oncanceled.length);
        }
        if (oncanceled.equals()) {
            int i6 = length + 41;
            getMin = i6 % 128;
            if (i6 % 2 != 0) {
                min.equals(oncanceled.setMin, oncanceled.length);
                Object[] objArr = null;
                int length2 = objArr.length;
            } else {
                min.equals(oncanceled.setMin, oncanceled.length);
            }
        }
        if (oncanceled.isInside()) {
            int i7 = length + 105;
            getMin = i7 % 128;
            int i8 = i7 % 2;
            min.getMax(oncanceled.setMin, oncanceled.length);
        }
        if (!oncanceled.toIntRange()) {
            z = true;
        }
        if (!z) {
            int i9 = length + 59;
            getMin = i9 % 128;
            int i10 = i9 % 2;
            min.toFloatRange(oncanceled.setMin, oncanceled.length);
        }
    }

    private static String setMin(char[] cArr, int i, byte b) {
        char[] cArr2 = setMax;
        char c = getMax;
        char[] cArr3 = new char[i];
        if ((i % 2 != 0 ? 'G' : ':') == 'G') {
            try {
                int i2 = length + 59;
                getMin = i2 % 128;
                int i3 = i2 % 2;
                i--;
                cArr3[i] = (char) (cArr[i] - b);
            } catch (Exception e) {
                throw e;
            }
        }
        if (i > 1) {
            for (int i4 = 0; i4 < i; i4 += 2) {
                char c2 = cArr[i4];
                int i5 = i4 + 1;
                char c3 = cArr[i5];
                if (c2 == c3) {
                    int i6 = getMin + 87;
                    length = i6 % 128;
                    int i7 = i6 % 2;
                    cArr3[i4] = (char) (c2 - b);
                    cArr3[i5] = (char) (c3 - b);
                    int i8 = getMin + 87;
                    length = i8 % 128;
                    int i9 = i8 % 2;
                } else {
                    int min = onActivityPreStopped.setMin(c2, c);
                    int length2 = onActivityPreStopped.length(c2, c);
                    int min2 = onActivityPreStopped.setMin(c3, c);
                    int length3 = onActivityPreStopped.length(c3, c);
                    if (length2 != length3) {
                        if (min == min2) {
                            int max = onActivityPreStopped.setMax(length2, c);
                            int max2 = onActivityPreStopped.setMax(length3, c);
                            int max3 = onActivityPreStopped.getMax(min, max, c);
                            int max4 = onActivityPreStopped.getMax(min2, max2, c);
                            cArr3[i4] = cArr2[max3];
                            cArr3[i5] = cArr2[max4];
                        } else {
                            int max5 = onActivityPreStopped.getMax(min, length3, c);
                            int max6 = onActivityPreStopped.getMax(min2, length2, c);
                            cArr3[i4] = cArr2[max5];
                            cArr3[i5] = cArr2[max6];
                        }
                    } else {
                        int max7 = onActivityPreStopped.setMax(min, c);
                        int max8 = onActivityPreStopped.setMax(min2, c);
                        int max9 = onActivityPreStopped.getMax(max7, length2, c);
                        int max10 = onActivityPreStopped.getMax(max8, length3, c);
                        cArr3[i4] = cArr2[max9];
                        cArr3[i5] = cArr2[max10];
                    }
                }
            }
        }
        return new String(cArr3);
    }
}
