package o;

import androidx.annotation.NonNull;
import androidx.lifecycle.Lifecycle;

public interface setPopupCallback extends showsIcon {
    void setMax(@NonNull setChecked setchecked, @NonNull Lifecycle.Event event);
}
