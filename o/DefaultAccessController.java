package o;

import java.util.List;

final class DefaultAccessController implements RedDotDrawable {
    private final isRemoteCallExtension getMin;

    public DefaultAccessController(isRemoteCallExtension isremotecallextension) {
        this.getMin = isremotecallextension;
    }

    public final Object apply(Object obj) {
        return this.getMin.lambda$getRecentRecipient$0((List) obj);
    }
}
