package o;

final class ImageUtil implements RedDotDrawable {
    private final getScheduledExecutor setMin;

    public ImageUtil(getScheduledExecutor getscheduledexecutor) {
        this.setMin = getscheduledexecutor;
    }

    public final Object apply(Object obj) {
        return this.setMin.apply(obj);
    }
}
