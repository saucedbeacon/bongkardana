package o;

import id.dana.richview.splitbill.SelectedPayerView;
import java.util.List;
import o.CameraView;

public final class setRecordPhase implements CameraView.length {
    private final SelectedPayerView length;

    public setRecordPhase(SelectedPayerView selectedPayerView) {
        this.length = selectedPayerView;
    }

    public final void getMax(List list) {
        SelectedPayerView.length(this.length, list);
    }
}
