package o;

import o.onDelete;

public interface openRecordMode {

    public interface setMax extends onDelete.length {
        void enableBanner(boolean z);

        void onLoadBanner(String str);
    }

    public interface setMin extends onDelete.getMin {
        void getMin();

        void setMin();
    }
}
