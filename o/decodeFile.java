package o;

import android.content.Context;

public final class decodeFile extends decodeByteArray {
    public decodeFile(Context context, setCancelOnTouchOutside$core setcancelontouchoutside_core, fillPageCommonInfo fillpagecommoninfo) {
        super(context, setcancelontouchoutside_core, fillpagecommoninfo);
    }

    /* access modifiers changed from: protected */
    public final String length() {
        postWebViewMessage bizInfo = setMax().getBizInfo();
        StringBuilder sb = new StringBuilder("https://m.dana.id");
        sb.append(setH5OptionMenuTextFlag.setMin("/m/parking/confirmation?ticketNumber={qrCode}").getMin("qrCode", bizInfo.invoke).getMax());
        return sb.toString();
    }
}
