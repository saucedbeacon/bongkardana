package o;

import dagger.internal.InjectedFieldSignature;
import id.dana.myprofile.MyProfileFragment;
import o.AUTextView;
import o.GriverAppLanguageExtensionImpl;
import o.OnTouch;
import o.fakeDragBy;
import o.getSelectedIndex;
import o.getTransitionProperties;
import o.onNext;
import o.parameters;
import o.setInitScene;
import o.updateScreenRotation;

public final class queryTrackerConfig implements getBindingAdapter<MyProfileFragment> {
    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.getBalancePresenter")
    public static void getMin(MyProfileFragment myProfileFragment, parameters.setMax setmax) {
        myProfileFragment.getBalancePresenter = setmax;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.getStaticQrPresenter")
    public static void setMax(MyProfileFragment myProfileFragment, OnTouch.setMin setmin) {
        myProfileFragment.getStaticQrPresenter = setmin;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.getUserStatementPresenter")
    public static void getMax(MyProfileFragment myProfileFragment, AUTextView.OnVisibilityChangeListener.setMax setmax) {
        myProfileFragment.getUserStatementPresenter = setmax;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.knowledgeBasedInfoManager")
    public static void setMin(MyProfileFragment myProfileFragment, getAppPerfKey getappperfkey) {
        myProfileFragment.knowledgeBasedInfoManager = getappperfkey;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.myProfilePresenter")
    public static void getMax(MyProfileFragment myProfileFragment, setInitScene.setMin setmin) {
        myProfileFragment.myProfilePresenter = setmin;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.profileQrDeepLinkPresenter")
    public static void length(MyProfileFragment myProfileFragment, getTransitionProperties.setMin setmin) {
        myProfileFragment.profileQrDeepLinkPresenter = setmin;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.queryPayPresenter")
    public static void setMax(MyProfileFragment myProfileFragment, fakeDragBy.length length) {
        myProfileFragment.queryPayPresenter = length;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.sessionExpiredManager")
    public static void setMax(MyProfileFragment myProfileFragment, CustomPopMenuExtension customPopMenuExtension) {
        myProfileFragment.sessionExpiredManager = customPopMenuExtension;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.bottomSheetOnBoardingPresenter")
    public static void getMax(MyProfileFragment myProfileFragment, getSelectedIndex.length length) {
        myProfileFragment.bottomSheetOnBoardingPresenter = length;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.globalNetworkPresenter")
    public static void setMax(MyProfileFragment myProfileFragment, onNext.setMin setmin) {
        myProfileFragment.globalNetworkPresenter = setmin;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.savingPresenter")
    public static void setMin(MyProfileFragment myProfileFragment, updateScreenRotation.setMin setmin) {
        myProfileFragment.savingPresenter = setmin;
    }

    @InjectedFieldSignature("id.dana.myprofile.MyProfileFragment.investmentPresenter")
    public static void length(MyProfileFragment myProfileFragment, GriverAppLanguageExtensionImpl.length length) {
        myProfileFragment.investmentPresenter = length;
    }
}
