package o;

import android.content.Context;
import android.widget.ImageView;
import o.RewriteQueriesToDropUnusedColumns;
import o.setOnMenuWindowDismissListener;

public final class setOnShowListener extends getDismissAnimationId {
    setOnShowListener(String str, setOnMenuWindowDismissListener.length length) {
        super(str, length);
    }

    public final void length(Context context, ImageView imageView) {
        RewriteQueriesToDropUnusedColumns.getMax getmax = new RewriteQueriesToDropUnusedColumns.getMax(context, (byte) 0);
        getmax.getMin = getMax();
        getmax.setMax = setMax();
        if (getMin() != null) {
            getmax.length = getMin();
        }
        getmax.setMin = imageView;
        new RewriteQueriesToDropUnusedColumns(getmax, (byte) 0);
    }
}
