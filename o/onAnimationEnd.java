package o;

import id.dana.oauth.model.OauthParamsModel;
import o.getPlatformCallback;
import o.onDelete;

public interface onAnimationEnd {

    public interface getMin extends onDelete.length {
        void setMax(OauthParamsModel oauthParamsModel);
    }

    public interface length extends onDelete.getMin {
        void getMax(getPlatformCallback.length length);

        void getMin(getPlatformCallback.getMin getmin);

        void length(isForMainFrame isformainframe);

        void setMax(String str, getPlatformCallback.setMin setmin);

        void setMin(String str, getPlatformCallback.setMax setmax);
    }
}
