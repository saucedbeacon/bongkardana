package o;

import com.alibaba.ariver.remotedebug.utils.TinyAppLogUtil;
import com.facebook.flipper.core.FlipperObject;
import com.google.firebase.analytics.FirebaseAnalytics;
import com.zoloz.android.phone.zdoc.service.ZdocRecordService;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.UUID;
import javax.annotation.Nullable;
import o.CoordinatorLayout;
import o.access$getStartingWakeLock$cp;
import o.increaseBuffer;
import o.onStartJob;

public class onHandleWork implements increaseBuffer {
    @Nullable
    public onStartJob setMax;

    public onHandleWork() {
        this.setMax = null;
    }

    public onHandleWork(onStartJob onstartjob) {
        this.setMax = onstartjob;
    }

    public final InvalidHandshakeException getMax(increaseBuffer.setMax setmax) throws IOException {
        InvalidDataException min = setmax.getMin();
        String obj = UUID.randomUUID().toString();
        onStartJob onstartjob = this.setMax;
        List<access$getStartingWakeLock$cp.getMin> max = setMax(min.length);
        access$getStartingWakeLock$cp.setMax setmax2 = new access$getStartingWakeLock$cp.setMax();
        setmax2.getMin = obj;
        setmax2.getMax = System.currentTimeMillis();
        setmax2.setMax = max;
        setmax2.length = min.getMin;
        setmax2.setMin = min.setMin.toString();
        if (min.setMax != null) {
            setmax2.toIntRange = setMax(min);
        }
        onstartjob.send("newRequest", new FlipperObject(new FlipperObject.setMax().getMax("id", setmax2.getMin).length(TinyAppLogUtil.TINY_APP_STANDARD_EXTRA_TIMESTAMP, Long.valueOf(setmax2.getMax)).getMax(FirebaseAnalytics.Param.METHOD, setmax2.length).getMax("url", setmax2.setMin).getMin("headers", onStartJob.getMax(setmax2.setMax)).getMax("data", onStartJob.setMin(setmax2.toIntRange)).getMax));
        InvalidHandshakeException max2 = setmax.getMax(min);
        NotSendableException notSendableException = max2.toFloatRange;
        List<access$getStartingWakeLock$cp.getMin> max3 = setMax(max2.toIntRange);
        access$getStartingWakeLock$cp.setMin setmin = new access$getStartingWakeLock$cp.setMin();
        setmin.setMax = obj;
        setmin.getMin = max2.hashCode;
        setmin.setMin = max2.getMax;
        setmin.getMax = max3;
        schedule max4 = notSendableException.getMax();
        max4.getMin(Long.MAX_VALUE);
        setmin.toIntRange = max4.getMax().clone().valueOf();
        onStartJob onstartjob2 = this.setMax;
        onStartJob.AnonymousClass2 r1 = new CoordinatorLayout.DispatchChangeEvent(onstartjob2.getConnection(), setmin) {
            final /* synthetic */ access$getStartingWakeLock$cp.setMin setMin;

            {
                this.setMin = r3;
            }

            public final void runOrThrow() throws Exception {
                if (onStartJob.setMax(this.setMin)) {
                    this.setMin.toIntRange = null;
                }
                onStartJob.this.send("newResponse", new FlipperObject(new FlipperObject.setMax().getMax("id", this.setMin.setMax).length(TinyAppLogUtil.TINY_APP_STANDARD_EXTRA_TIMESTAMP, Long.valueOf(this.setMin.getMin)).getMin("status", Integer.valueOf(this.setMin.setMin)).getMax(ZdocRecordService.REASON, this.setMin.length).getMin("headers", onStartJob.getMax(this.setMin.getMax)).getMax("data", onStartJob.setMax(this.setMin.toIntRange)).getMax));
            }
        };
        if (onstartjob2.setMin != null) {
            Iterator<access$getStartingWakeLockLock$cp> it = onstartjob2.setMin.iterator();
            while (true) {
                if (it.hasNext()) {
                    if (it.next().getMax()) {
                        new Object(setmin, r1) {
                            final /* synthetic */ access$getStartingWakeLock$cp.setMin getMax;
                            final /* synthetic */ Runnable setMin;

                            {
                                this.getMax = r2;
                                this.setMin = r3;
                            }
                        };
                        break;
                    }
                } else {
                    break;
                }
            }
        }
        r1.run();
        return max2;
    }

    private static byte[] setMax(InvalidDataException invalidDataException) throws IOException {
        submitLazy submitlazy = new submitLazy();
        invalidDataException.setMax.getMin(submitlazy);
        return submitlazy.valueOf();
    }

    private static List<access$getStartingWakeLock$cp.getMin> setMax(Draft_75 draft_75) {
        ArrayList arrayList = new ArrayList();
        for (String next : draft_75.getMin()) {
            arrayList.add(new access$getStartingWakeLock$cp.getMin(next, draft_75.length(next)));
        }
        return arrayList;
    }
}
