package o;

import javax.inject.Inject;
import o.TitleBarRightButtonView;

public class BleGattService extends parseFailed<getDataFieldCount, Void> {
    private static String BANK = "BANK_TRANSFER";
    private final fromInt sendMoneyRepository;

    @Inject
    public BleGattService(appxLoadFailed appxloadfailed, getScheme getscheme, fromInt fromint) {
        super(appxloadfailed, getscheme);
        this.sendMoneyRepository = fromint;
    }

    /* access modifiers changed from: protected */
    public TitleBarRightButtonView.AnonymousClass1<getDataFieldCount> buildUseCaseObservable(Void voidR) {
        return this.sendMoneyRepository.getTransferOption(BANK);
    }
}
