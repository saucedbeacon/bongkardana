package o;

import android.os.Bundle;
import android.view.View;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.fragment.app.Fragment;
import androidx.fragment.app.FragmentManager;
import java.util.Iterator;
import java.util.concurrent.CopyOnWriteArrayList;

public final class getMinimumHeight {
    @NonNull
    private final FragmentManager getMin;
    @NonNull
    public final CopyOnWriteArrayList<length> setMin = new CopyOnWriteArrayList<>();

    public static final class length {
        final boolean getMax = false;
        @NonNull
        public final FragmentManager.setMax setMin;

        public length(@NonNull FragmentManager.setMax setmax) {
            this.setMin = setmax;
        }
    }

    public getMinimumHeight(@NonNull FragmentManager fragmentManager) {
        this.getMin = fragmentManager;
    }

    /* access modifiers changed from: package-private */
    public final void setMin(@NonNull Fragment fragment) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.setMin(fragment);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    /* access modifiers changed from: package-private */
    public final void getMax(@NonNull Fragment fragment) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.getMax(fragment);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    /* access modifiers changed from: package-private */
    public final void setMin(@NonNull Fragment fragment, @Nullable Bundle bundle) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.setMin(fragment, bundle);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    /* access modifiers changed from: package-private */
    public final void getMin(@NonNull Fragment fragment, @Nullable Bundle bundle) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.getMin(fragment, bundle);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    /* access modifiers changed from: package-private */
    public final void length(@NonNull Fragment fragment, @Nullable Bundle bundle) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.length(fragment, bundle);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    /* access modifiers changed from: package-private */
    public final void getMax(@NonNull Fragment fragment, @NonNull View view, @Nullable Bundle bundle, boolean z) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.getMax(fragment, view, bundle, true);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            length next = it.next();
            if (!z || next.getMax) {
                next.setMin.getMin(this.getMin, fragment, view);
            }
        }
    }

    public final void length(@NonNull Fragment fragment) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.length(fragment);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    /* access modifiers changed from: package-private */
    public final void getMin(@NonNull Fragment fragment) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.getMin(fragment);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    public final void setMax(@NonNull Fragment fragment) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.setMax(fragment);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    public final void equals(@NonNull Fragment fragment) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.equals(fragment);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    /* access modifiers changed from: package-private */
    public final void setMax(@NonNull Fragment fragment, @NonNull Bundle bundle) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.setMax(fragment, bundle);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    public final void IsOverlapping(@NonNull Fragment fragment) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.IsOverlapping(fragment);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    /* access modifiers changed from: package-private */
    public final void toFloatRange(@NonNull Fragment fragment) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.toFloatRange(fragment);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }

    /* access modifiers changed from: package-private */
    public final void isInside(@NonNull Fragment fragment) {
        Fragment fragment2 = this.getMin.values;
        if (fragment2 != null) {
            fragment2.getParentFragmentManager().equals.isInside(fragment);
        }
        Iterator<length> it = this.setMin.iterator();
        while (it.hasNext()) {
            it.next();
        }
    }
}
