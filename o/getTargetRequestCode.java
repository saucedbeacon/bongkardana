package o;

public final class getTargetRequestCode extends getActivity {
    public getTargetRequestCode(setTargetFragment settargetfragment) {
        super(settargetfragment);
    }

    public final void getMax(boolean z) {
        this.setMin.reset();
        if (!z) {
            this.setMin.postTranslate(this.length.setMin(), this.length.Grayscale$Algorithm() - this.length.getMin());
            return;
        }
        this.setMin.setTranslate(-(this.length.invoke() - this.length.length()), this.length.Grayscale$Algorithm() - this.length.getMin());
        this.setMin.postScale(-1.0f, 1.0f);
    }
}
