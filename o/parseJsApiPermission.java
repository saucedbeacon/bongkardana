package o;

import o.addAnimatorUpdateListener;

final class parseJsApiPermission implements addAnimatorUpdateListener.getMax {
    private final PermissionManager setMax;

    public parseJsApiPermission(PermissionManager permissionManager) {
        this.setMax = permissionManager;
    }

    public final Object processFacade(Object obj) {
        return ((RVFlag) obj).restore(this.setMax);
    }
}
