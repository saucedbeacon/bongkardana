package o;

final class getScene implements RedDotDrawable {
    private final getOrigin getMin;
    private final generateFromJSON length;

    public getScene(getOrigin getorigin, generateFromJSON generatefromjson) {
        this.getMin = getorigin;
        this.length = generatefromjson;
    }

    public final Object apply(Object obj) {
        return this.getMin.lambda$getUserConfig$11(this.length, (getPluginModels) obj);
    }
}
