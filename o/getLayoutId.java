package o;

import android.annotation.SuppressLint;
import android.content.Context;
import android.graphics.drawable.Drawable;
import android.widget.ImageView;
import androidx.annotation.LayoutRes;
import com.bumptech.glide.Glide;
import o.ITinyMenuPopupWindow;
import o.setOnMenuWindowDismissListener;

public final class getLayoutId extends ITinyMenuPopupWindow.OnMenuWindowDismissListener<Drawable> {
    getLayoutId(String str, @LayoutRes int i, setOnMenuWindowDismissListener.length length) {
        super(str, i, length);
    }

    /* access modifiers changed from: protected */
    @SuppressLint({"CheckResult"})
    public final void getMin(Context context, ImageView imageView) {
        if (this.getMin != null) {
            this.getMin.getMin();
        }
        updateMessageMenuItem<Drawable> min = ((updateCornerMarking) Glide.getMax(context)).setMax(getMax()).setMax(evaluate.setMin).setMax(setMax()).setMin(setMax());
        if (setMin() != null) {
            min.setMin((parseBehavior<Drawable>) setMin());
        }
        min.length(imageView);
    }
}
