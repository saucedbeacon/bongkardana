package o;

import o.onDelete;

public interface enableLayers {

    public interface getMin extends onDelete.length {
        void getMax(Boolean bool);
    }

    public interface setMin extends onDelete.getMin {
        void getMax();
    }
}
