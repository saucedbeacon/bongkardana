package o;

import android.content.Context;
import android.graphics.Rect;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;
import android.view.WindowManager;
import android.widget.TextView;
import androidx.annotation.NonNull;
import androidx.annotation.RestrictTo;
import o.ITrustedWebActivityCallback;

@RestrictTo({RestrictTo.Scope.LIBRARY_GROUP_PREFIX})
final class prepare {
    final int[] IsOverlapping = new int[2];
    final View getMax;
    final Rect getMin = new Rect();
    final WindowManager.LayoutParams length = new WindowManager.LayoutParams();
    final Context setMax;
    final TextView setMin;
    final int[] toIntRange = new int[2];

    prepare(@NonNull Context context) {
        this.setMax = context;
        View inflate = LayoutInflater.from(context).inflate(ITrustedWebActivityCallback.Stub.equals.onNavigationEvent, (ViewGroup) null);
        this.getMax = inflate;
        this.setMin = (TextView) inflate.findViewById(ITrustedWebActivityCallback.Stub.toFloatRange.ICustomTabsCallback);
        this.length.setTitle(getClass().getSimpleName());
        this.length.packageName = this.setMax.getPackageName();
        this.length.type = 1002;
        this.length.width = -2;
        this.length.height = -2;
        this.length.format = -3;
        this.length.windowAnimations = ITrustedWebActivityCallback.Stub.isInside.getMax;
        this.length.flags = 24;
    }

    /* access modifiers changed from: package-private */
    public final boolean getMax() {
        return this.getMax.getParent() != null;
    }
}
