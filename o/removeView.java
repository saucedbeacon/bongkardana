package o;

import android.graphics.Bitmap;
import androidx.annotation.NonNull;
import java.security.MessageDigest;

public final class removeView extends setType {
    private static final byte[] setMax = "com.bumptech.glide.load.resource.bitmap.FitCenter".getBytes(setMin);

    /* access modifiers changed from: protected */
    public final Bitmap setMax(@NonNull rebuildScene rebuildscene, @NonNull Bitmap bitmap, int i, int i2) {
        return setTag.setMax(rebuildscene, bitmap, i, i2);
    }

    public final boolean equals(Object obj) {
        return obj instanceof removeView;
    }

    public final int hashCode() {
        return "com.bumptech.glide.load.resource.bitmap.FitCenter".hashCode();
    }

    public final void setMax(@NonNull MessageDigest messageDigest) {
        messageDigest.update(setMax);
    }
}
