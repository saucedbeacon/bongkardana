package o;

import androidx.annotation.NonNull;
import androidx.annotation.RequiresApi;
import java.util.Arrays;

@RequiresApi(21)
public final class setItemViewCacheSize extends initAdapterManager {
    private final setScrollingTouchSlop getMax = new addRecyclerListener(Arrays.asList(new setScrollingTouchSlop[]{new getOnFlingListener(), new getLayoutManager(), new setViewCacheExtension()}));

    @NonNull
    public final setScrollingTouchSlop getMax() {
        return this.getMax;
    }
}
