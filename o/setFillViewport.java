package o;

public final class setFillViewport {
    static boolean getMax(ensureProcessorRunningLocked ensureprocessorrunninglocked, isStopped isstopped) {
        return onOverScrolled.getMin(ensureprocessorrunninglocked, isstopped);
    }

    static boolean setMax(isStopped isstopped) {
        return isstopped.equals;
    }
}
