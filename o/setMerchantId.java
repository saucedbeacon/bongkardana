package o;

import android.content.Context;
import androidx.work.ListenableWorker;
import androidx.work.WorkerParameters;

public interface setMerchantId {
    ListenableWorker getMax(Context context, WorkerParameters workerParameters);
}
