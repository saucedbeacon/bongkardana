package o;

final class isOnlineScene implements RedDotDrawable {
    private final getOrigin getMax;
    private final setValidDomains length;

    public isOnlineScene(getOrigin getorigin, setValidDomains setvaliddomains) {
        this.getMax = getorigin;
        this.length = setvaliddomains;
    }

    public final Object apply(Object obj) {
        return this.getMax.lambda$saveUserConfigToRemote$7(this.length, (Boolean) obj);
    }
}
