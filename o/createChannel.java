package o;

import com.alipayplus.mobile.component.domain.model.request.BaseRpcRequest;
import o.addAnimatorUpdateListener;

final class createChannel implements addAnimatorUpdateListener.getMax {
    private final BaseRpcRequest length;

    public createChannel(BaseRpcRequest baseRpcRequest) {
        this.length = baseRpcRequest;
    }

    public final Object processFacade(Object obj) {
        return ((releaseAllChannel) obj).statusConsult(this.length);
    }
}
