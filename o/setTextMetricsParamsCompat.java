package o;

import android.content.ComponentName;
import android.content.Context;
import androidx.annotation.NonNull;

public final class setTextMetricsParamsCompat {
    private static final String getMin = setSupportBackgroundTintList.length("PackageManagerHelper");

    public static void setMax(@NonNull Context context, @NonNull Class<?> cls, boolean z) {
        try {
            context.getPackageManager().setComponentEnabledSetting(new ComponentName(context, cls.getName()), z ? 1 : 2, 1);
            setSupportBackgroundTintList.getMax();
            cls.getName();
        } catch (Exception e) {
            setSupportBackgroundTintList.getMax();
            cls.getName();
            new Throwable[1][0] = e;
        }
    }
}
