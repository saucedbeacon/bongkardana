package o;

import com.airbnb.lottie.parser.moshi.JsonReader;
import java.io.IOException;
import java.util.ArrayList;

final class toggle {
    private static JsonReader.setMax getMax = JsonReader.setMax.getMin("nm", "hd", "it");

    static showSoftInputIfNecessary getMin(JsonReader jsonReader, setVerticalGravity setverticalgravity) throws IOException {
        ArrayList arrayList = new ArrayList();
        String str = null;
        boolean z = false;
        while (jsonReader.getMin()) {
            int min = jsonReader.setMin(getMax);
            if (min == 0) {
                str = jsonReader.toIntRange();
            } else if (min == 1) {
                z = jsonReader.equals();
            } else if (min != 2) {
                jsonReader.FastBitmap$CoordinateSystem();
            } else {
                jsonReader.length();
                while (jsonReader.getMin()) {
                    isLandscapeMode min2 = applyTrackTint.setMin(jsonReader, setverticalgravity);
                    if (min2 != null) {
                        arrayList.add(min2);
                    }
                }
                jsonReader.getMax();
            }
        }
        return new showSoftInputIfNecessary(str, arrayList, z);
    }
}
