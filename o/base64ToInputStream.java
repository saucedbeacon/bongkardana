package o;

final class base64ToInputStream implements RedDotDrawable {
    public static final base64ToInputStream getMax = new base64ToInputStream();

    public final Object apply(Object obj) {
        return FileUtils.toEWalletInovConfig((currentThreadInArray) obj);
    }
}
