package o;

import com.google.android.exoplayer2.extractor.ts.TsExtractor;
import java.util.ArrayList;
import java.util.List;

public final class setDeepLiveness extends isHasFace {
    public isHasFace equals;
    public List<CircleFrameLayout> isInside = new ArrayList();

    public final boolean toDoubleRange() {
        return true;
    }

    public setDeepLiveness() {
        this.getMin = TsExtractor.TS_STREAM_TYPE_SPLICE_INFO;
    }

    public setDeepLiveness(int i) {
        super(i);
        this.getMin = TsExtractor.TS_STREAM_TYPE_SPLICE_INFO;
    }

    public final CircleFrameLayout getMax(String str) {
        for (CircleFrameLayout next : this.isInside) {
            if (str.equals(next.isInside)) {
                return next;
            }
        }
        return null;
    }
}
