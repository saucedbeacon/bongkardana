package o;

import javax.inject.Inject;
import o.TitleBarRightButtonView;

public class isRegionStatePersistenceEnabled extends parseFailed<getPowerCorrection, Void> {
    private final fromInt sendMoneyRepository;

    @Inject
    public isRegionStatePersistenceEnabled(appxLoadFailed appxloadfailed, getScheme getscheme, fromInt fromint) {
        super(appxloadfailed, getscheme);
        this.sendMoneyRepository = fromint;
    }

    /* access modifiers changed from: protected */
    public TitleBarRightButtonView.AnonymousClass1<getPowerCorrection> buildUseCaseObservable(Void voidR) {
        return this.sendMoneyRepository.getSendMoneyOption();
    }
}
