package o;

import o.addAnimatorUpdateListener;
import o.inflate;

final class setLastHeight implements addAnimatorUpdateListener.getMax {
    private final waitForWidth getMin;

    public setLastHeight(waitForWidth waitforwidth) {
        this.getMin = waitforwidth;
    }

    public final Object processFacade(Object obj) {
        return ((inflate.Cdefault) obj).oauthAuthCodeApply(this.getMin);
    }
}
