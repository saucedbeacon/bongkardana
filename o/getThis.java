package o;

final class getThis implements RedDotDrawable {
    private final lock getMin;

    public getThis(lock lock) {
        this.getMin = lock;
    }

    public final Object apply(Object obj) {
        return this.getMin.lambda$thenRegister$13((setEventName) obj);
    }
}
