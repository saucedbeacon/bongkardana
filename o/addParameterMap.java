package o;

public class addParameterMap extends setSpeed<intercept, getEmbedView> {
    public getEmbedView apply(intercept intercept) {
        return map(intercept);
    }

    /* access modifiers changed from: protected */
    public getEmbedView map(intercept intercept) {
        if (intercept == null) {
            return null;
        }
        getEmbedView getembedview = new getEmbedView();
        getembedview.setActivityId(intercept.getActivityId());
        return getembedview;
    }
}
