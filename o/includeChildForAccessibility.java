package o;

import com.github.mikephil.charting.charts.PieChart;

public final class includeChildForAccessibility extends onKeyUp<PieChart> {
    public includeChildForAccessibility(PieChart pieChart) {
        super(pieChart);
    }

    /* access modifiers changed from: protected */
    public final addFocusables getMin(int i, float f, float f2) {
        getEmojiReplaceStrategy doubleRange = ((getStatusBarBackgroundDrawable) ((PieChart) this.setMax).getData()).toDoubleRange();
        return new addFocusables((float) i, doubleRange.toIntRange(i).getMin(), f, f2, 0, doubleRange.getCause());
    }
}
