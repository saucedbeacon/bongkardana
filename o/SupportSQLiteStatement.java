package o;

import androidx.core.view.ViewCompat;
import androidx.recyclerview.widget.RecyclerView;
import o.InitializationProvider;

public class SupportSQLiteStatement<T> extends InitializationProvider<T> {
    public void IsOverlapping(RecyclerView.valueOf valueof) {
        ViewCompat.getMin(valueof.itemView, 0.0f);
    }

    public void isInside(RecyclerView.valueOf valueof) {
        ViewCompat.getMin(valueof.itemView, 1.0f);
    }

    public void toIntRange(RecyclerView.valueOf valueof) {
        ViewCompat.getMin(valueof.itemView, 1.0f);
    }

    public final void toFloatRange(RecyclerView.valueOf valueof) {
        ViewCompat.getMin(valueof.itemView, 1.0f);
    }

    public invalidateOptionsMenu values(RecyclerView.valueOf valueof) {
        return ViewCompat.Grayscale$Algorithm(valueof.itemView).getMin(getMin()).getMin(0.0f).setMin(this.getMax);
    }

    public invalidateOptionsMenu toDoubleRange(RecyclerView.valueOf valueof) {
        return ViewCompat.Grayscale$Algorithm(valueof.itemView).getMin(1.0f).getMin(this.toIntRange).setMin(this.getMax);
    }

    public final invalidateOptionsMenu length(RecyclerView.valueOf valueof, InitializationProvider.getMin getmin) {
        return ViewCompat.Grayscale$Algorithm(valueof.itemView).getMin(this.toDoubleRange).getMin(0.0f).setMin((float) (getmin.setMin - getmin.length)).getMax((float) (getmin.toIntRange - getmin.getMin)).setMin(this.getMax);
    }

    public final invalidateOptionsMenu equals(RecyclerView.valueOf valueof) {
        return ViewCompat.Grayscale$Algorithm(valueof.itemView).setMin(0.0f).getMax(0.0f).getMin(this.toDoubleRange).getMin(1.0f).setMin(this.getMax);
    }
}
