package o;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import com.otaliastudios.cameraview.CameraLogger;
import o.calculateDtToFit;
import o.onPointerUp;

public abstract class removeOnItemTouchListener extends onPointerUp {
    protected static final CameraLogger getMax;
    private static final String setMin;

    static {
        String simpleName = removeOnItemTouchListener.class.getSimpleName();
        setMin = simpleName;
        getMax = CameraLogger.setMin(simpleName);
    }

    public removeOnItemTouchListener(@NonNull calculateDtToFit.length length, @Nullable onPointerUp.length length2) {
        super(length, length2);
    }
}
