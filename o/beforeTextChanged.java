package o;

import o.addAnimatorUpdateListener;
import o.inflate;

final class beforeTextChanged implements addAnimatorUpdateListener.getMax {
    private final waitForWidth setMax;

    public beforeTextChanged(waitForWidth waitforwidth) {
        this.setMax = waitforwidth;
    }

    public final Object processFacade(Object obj) {
        return ((inflate.Cdefault) obj).oauthAuthCodeApply(this.setMax);
    }
}
