package o;

import o.addAnimatorUpdateListener;

final class RemoteDebugStateListener implements addAnimatorUpdateListener.getMax {
    private final connect length;

    public RemoteDebugStateListener(connect connect) {
        this.length = connect;
    }

    public final Object processFacade(Object obj) {
        return ((onSocketMessage) obj).getSplitBillHistories(this.length);
    }
}
