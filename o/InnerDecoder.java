package o;

import android.content.Context;

public final class InnerDecoder extends decodeByteArray {
    public InnerDecoder(Context context, setCancelOnTouchOutside$core setcancelontouchoutside_core, fillPageCommonInfo fillpagecommoninfo) {
        super(context, setcancelontouchoutside_core, fillpagecommoninfo);
    }

    /* access modifiers changed from: protected */
    public final String length() {
        return (setMax() == null || setMax().getBizInfo() == null || setMax().getBizInfo().valueOf == null) ? "" : setMax().getBizInfo().valueOf;
    }
}
