package o;

import java.util.concurrent.Callable;

final class pushPage implements Callable {
    public static final pushPage setMin = new pushPage();

    public final Object call() {
        return handleMessage.lambda$getCertificatesUrl$4();
    }
}
