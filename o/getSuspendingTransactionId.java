package o;

import android.graphics.drawable.PictureDrawable;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import com.caverock.androidsvg.SVG;

public final class getSuspendingTransactionId implements setConstraintSet<SVG, PictureDrawable> {
    @Nullable
    public final fireTrigger<PictureDrawable> length(@NonNull fireTrigger<SVG> firetrigger, @NonNull MotionHelper motionHelper) {
        return new getRoundPercent(new PictureDrawable(firetrigger.setMin().renderToPicture()));
    }
}
