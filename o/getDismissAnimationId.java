package o;

import o.setOnMenuWindowDismissListener;

abstract class getDismissAnimationId extends setOnMenuWindowDismissListener {
    private final String getMax;

    getDismissAnimationId(String str, setOnMenuWindowDismissListener.length length) {
        super(length);
        this.getMax = str;
    }

    /* access modifiers changed from: package-private */
    public final String getMax() {
        return this.getMax;
    }
}
