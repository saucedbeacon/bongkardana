package o;

import o.addAnimatorUpdateListener;

final class sendSimpleRpc implements addAnimatorUpdateListener.getMax {
    private final getResponse setMax;

    public sendSimpleRpc(getResponse getresponse) {
        this.setMax = getresponse;
    }

    public final Object processFacade(Object obj) {
        return ((getCode) obj).fetchPublicKey(this.setMax);
    }
}
