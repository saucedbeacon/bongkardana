package o;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

public final class getOptimizationLevel implements setConstraintSet<getViewWidget, byte[]> {
    @Nullable
    public final fireTrigger<byte[]> length(@NonNull fireTrigger<getViewWidget> firetrigger, @NonNull MotionHelper motionHelper) {
        return new ConstraintLayout(setInsetOffsetY.length(firetrigger.setMin().getMax.getMin.getMax.length().asReadOnlyBuffer()));
    }
}
