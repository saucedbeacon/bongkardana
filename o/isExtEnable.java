package o;

import o.addAnimatorUpdateListener;

final class isExtEnable implements addAnimatorUpdateListener.getMax {
    private final getWindow getMax;

    public isExtEnable(getWindow getwindow) {
        this.getMax = getwindow;
    }

    public final Object processFacade(Object obj) {
        return ((setWindow) obj).updateUserInfo(this.getMax);
    }
}
