package o;

import o.addAnimatorUpdateListener;

final class startFallback implements addAnimatorUpdateListener.getMax {
    private final OfflineMode setMax;

    public startFallback(OfflineMode offlineMode) {
        this.setMax = offlineMode;
    }

    public final Object processFacade(Object obj) {
        return ((prefetchUrl) obj).getSecurityQuestionState(this.setMax);
    }
}
