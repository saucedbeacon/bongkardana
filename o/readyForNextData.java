package o;

import o.addAnimatorUpdateListener;

final class readyForNextData implements addAnimatorUpdateListener.getMax {
    private final pushData getMin;

    public readyForNextData(pushData pushdata) {
        this.getMin = pushdata;
    }

    public final Object processFacade(Object obj) {
        return ((releaseAllChannel) obj).consult(this.getMin);
    }
}
