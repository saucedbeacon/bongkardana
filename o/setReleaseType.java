package o;

import java.util.Collections;
import java.util.Map;
import java.util.TimeZone;

public final class setReleaseType extends getReleaseType {
    protected Map<Class<? extends Object>, setFmt> isInside = Collections.emptyMap();

    public final /* bridge */ /* synthetic */ void length(TimeZone timeZone) {
        super.length(timeZone);
    }

    public setReleaseType() {
        this.setMax.put((Object) null, new getMin());
    }

    public final void setMax(refreshSessionID refreshsessionid) {
        super.setMax(refreshsessionid);
        for (setFmt setfmt : this.isInside.values()) {
            setfmt.length = refreshsessionid;
        }
    }

    protected class getMin implements setLogAliveInterval {
        protected getMin() {
        }
    }
}
