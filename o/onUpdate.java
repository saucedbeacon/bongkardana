package o;

import o.onDelete;

public abstract class onUpdate<T> extends GriverAppVirtualHostProxy<T> {
    private final onDelete.getMax length;

    public onUpdate(onDelete.getMax getmax) {
        this.length = getmax;
    }

    public void onError(Throwable th) {
        super.onError(th);
    }
}
