package o;

import android.content.Context;
import android.os.Build;
import androidx.annotation.NonNull;
import androidx.work.NetworkType;

public final class setImageBitmap extends setImageDrawable<onTextContextMenuItem> {
    private static final String setMin = setSupportBackgroundTintList.length("NetworkMeteredCtrlr");

    /* access modifiers changed from: package-private */
    public final /* synthetic */ boolean getMin(@NonNull Object obj) {
        return setMin((onTextContextMenuItem) obj);
    }

    public setImageBitmap(Context context, getMinWidthMinor getminwidthminor) {
        super(AppCompatPopupWindow.setMax(context, getminwidthminor).getMin);
    }

    /* access modifiers changed from: package-private */
    public final boolean getMax(@NonNull getInternalPopup getinternalpopup) {
        return getinternalpopup.toFloatRange.length == NetworkType.METERED;
    }

    private static boolean setMin(@NonNull onTextContextMenuItem ontextcontextmenuitem) {
        if (Build.VERSION.SDK_INT >= 26) {
            return !ontextcontextmenuitem.getMax() || !ontextcontextmenuitem.getMin();
        }
        setSupportBackgroundTintList.getMax();
        return !ontextcontextmenuitem.getMax();
    }
}
