package o;

import java.util.Map;

final class interceptBackEvent implements getChildren {
    private final Map setMax;

    public interceptBackEvent(Map map) {
        this.setMax = map;
    }

    public final boolean test(Object obj) {
        return needPermissionCheck.lambda$getOrderedNewMerchantCategories$8(this.setMax, (String) obj);
    }
}
