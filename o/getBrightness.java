package o;

import android.content.Context;
import androidx.annotation.NonNull;
import java.security.MessageDigest;

public final class getBrightness<T> implements updatePostLayout<T> {
    private static final updatePostLayout<?> getMin = new getBrightness();

    @NonNull
    public final fireTrigger<T> getMax(@NonNull Context context, @NonNull fireTrigger<T> firetrigger, int i, int i2) {
        return firetrigger;
    }

    public final void setMax(@NonNull MessageDigest messageDigest) {
    }

    @NonNull
    public static <T> getBrightness<T> setMin() {
        return (getBrightness) getMin;
    }

    private getBrightness() {
    }
}
