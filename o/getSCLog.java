package o;

import java.util.regex.Pattern;

public final class getSCLog extends onUserConfirm {
    private static final Pattern getMin = Pattern.compile("(?:\n[ \t]*)+$");
    private final encodeFromFile getMax = new encodeFromFile();
    private createJavaLogImpl setMin = new createJavaLogImpl();

    public final SCNetworkAsyncTaskExecutorFactory getMax() {
        return this.getMax;
    }

    public final setRsaPublicKey setMax(setEncryptRandomStringAndType setencryptrandomstringandtype) {
        if (setencryptrandomstringandtype.toFloatRange() >= SSLExtensions.setMax) {
            return new isAndroidPlatform(-1, setencryptrandomstringandtype.length() + SSLExtensions.setMax, false);
        }
        if (setencryptrandomstringandtype.toIntRange()) {
            return new isAndroidPlatform(setencryptrandomstringandtype.setMax(), -1, false);
        }
        return null;
    }

    public final void getMax(CharSequence charSequence) {
        this.setMin.setMin(charSequence);
    }

    public final void I_() {
        this.setMin.setMin("");
        String obj = this.setMin.getMax.toString();
        this.setMin = null;
        this.getMax.getMin = getMin.matcher(obj).replaceFirst("\n");
    }

    public static class length extends EncryptRandomType {
        public final setNeedConfirmButton length(setEncryptRandomStringAndType setencryptrandomstringandtype, SafeInputContext safeInputContext) {
            if (setencryptrandomstringandtype.toFloatRange() < SSLExtensions.setMax || setencryptrandomstringandtype.toIntRange() || (setencryptrandomstringandtype.equals().getMax() instanceof suspendEncoding)) {
                return null;
            }
            return new createAndroidMPaaSLogImpl(new getSCLog()).getMax(setencryptrandomstringandtype.length() + SSLExtensions.setMax);
        }
    }
}
