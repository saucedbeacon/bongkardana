package o;

import javax.inject.Inject;
import o.TitleBarRightButtonView;

public class copyToFile extends parseFailed<H5UrlHelper, Void> {
    private final getCharsetName pushNotificationRepository;

    @Inject
    public copyToFile(appxLoadFailed appxloadfailed, getScheme getscheme, getCharsetName getcharsetname) {
        super(appxloadfailed, getscheme);
        this.pushNotificationRepository = getcharsetname;
    }

    /* access modifiers changed from: protected */
    public TitleBarRightButtonView.AnonymousClass1<H5UrlHelper> buildUseCaseObservable(Void voidR) {
        return this.pushNotificationRepository.bindApp();
    }
}
