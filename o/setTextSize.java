package o;

import android.annotation.SuppressLint;
import android.content.Context;
import androidx.annotation.NonNull;
import androidx.lifecycle.LiveData;
import androidx.work.ExistingPeriodicWorkPolicy;
import androidx.work.ExistingWorkPolicy;
import androidx.work.WorkInfo;
import com.google.common.util.concurrent.ListenableFuture;
import java.util.List;

@SuppressLint({"AddedAbstractMethod"})
public abstract class setTextSize {
    @NonNull
    public abstract onCreateInputConnection getMax(@NonNull String str);

    @NonNull
    public abstract LiveData<List<WorkInfo>> getMin(@NonNull String str);

    @NonNull
    public abstract setAutoSizeTextTypeUniformWithPresetSizes getMin(@NonNull String str, @NonNull ExistingWorkPolicy existingWorkPolicy, @NonNull List<setBackgroundDrawable> list);

    @NonNull
    public abstract onCreateInputConnection length(@NonNull String str, @NonNull ExistingPeriodicWorkPolicy existingPeriodicWorkPolicy, @NonNull setTextAppearance settextappearance);

    @NonNull
    public abstract onCreateInputConnection length(@NonNull List<? extends onTextChanged> list);

    @NonNull
    public abstract setAutoSizeTextTypeUniformWithPresetSizes setMax(@NonNull List<setBackgroundDrawable> list);

    @NonNull
    public abstract ListenableFuture<List<WorkInfo>> setMin(@NonNull String str);

    @NonNull
    public abstract onCreateInputConnection setMin(@NonNull String str, @NonNull ExistingWorkPolicy existingWorkPolicy, @NonNull List<setBackgroundDrawable> list);

    @NonNull
    public static setTextSize getMax(@NonNull Context context) {
        return setSupportCompoundDrawablesTintList.length(context);
    }

    public static void setMax(@NonNull Context context, @NonNull onAttachedToWindow onattachedtowindow) {
        setSupportCompoundDrawablesTintList.length(context, onattachedtowindow);
    }
}
