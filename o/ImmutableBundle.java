package o;

import o.addAnimatorUpdateListener;

final class ImmutableBundle implements addAnimatorUpdateListener.getMax {
    private final ImmutableList getMin;

    public ImmutableBundle(ImmutableList immutableList) {
        this.getMin = immutableList;
    }

    public final Object processFacade(Object obj) {
        return ((keySet) obj).getReferralTracker(this.getMin);
    }
}
