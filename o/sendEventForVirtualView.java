package o;

import android.view.Choreographer;

public interface sendEventForVirtualView {

    public static abstract class length {
        Runnable getMax;
        Choreographer.FrameCallback length;

        public abstract void getMax(long j);
    }

    void getMax(length length2);

    void length(length length2);

    void setMax(length length2, long j);
}
