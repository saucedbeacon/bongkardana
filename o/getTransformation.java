package o;

import java.util.concurrent.Callable;

final class getTransformation implements Callable {
    private final attach getMax;
    private final getFrameMarginVertical$core setMax;

    public getTransformation(getFrameMarginVertical$core getframemarginvertical_core, attach attach) {
        this.setMax = getframemarginvertical_core;
        this.getMax = attach;
    }

    public final Object call() {
        return this.setMax.lambda$saveAccount$2(this.getMax);
    }
}
