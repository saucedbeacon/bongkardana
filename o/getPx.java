package o;

import android.text.TextUtils;
import id.dana.tracker.TrackerKey;
import io.branch.referral.Branch;

public final class getPx implements getTypeface {
    private final getTypefaceByCat setMin;

    public final void setMin() {
    }

    public getPx(getTypefaceByCat gettypefacebycat) {
        this.setMin = gettypefacebycat;
    }

    public final void getMax() {
        getTypefaceByCat gettypefacebycat = this.setMin;
        if (gettypefacebycat == null) {
            return;
        }
        if (TextUtils.isEmpty(gettypefacebycat.setMax.setMin)) {
            String str = this.setMin.setMax.setMax;
            if (!TextUtils.isEmpty(str)) {
                Branch.setMin().setMax.getMax(TrackerKey.DefaultMixpanel.DISTINCT_ID, str);
                return;
            }
            return;
        }
        String str2 = this.setMin.setMax.setMin;
        if (!TextUtils.isEmpty(str2)) {
            Branch.setMin().setMax.getMax(TrackerKey.DefaultMixpanel.DISTINCT_ID, str2);
        }
    }

    public final void setMax() {
        String str = this.setMin.setMax.setMin;
        if (!TextUtils.isEmpty(str)) {
            Branch.setMin().setMax.getMax(TrackerKey.DefaultMixpanel.DISTINCT_ID, str);
        }
    }

    public final void length() {
        String str = this.setMin.setMax.setMin;
        if (!TextUtils.isEmpty(str)) {
            Branch.setMin().setMax.getMax(TrackerKey.DefaultMixpanel.DISTINCT_ID, str);
        }
    }
}
