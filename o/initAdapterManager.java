package o;

import android.hardware.camera2.CaptureRequest;
import android.hardware.camera2.CaptureResult;
import android.hardware.camera2.TotalCaptureResult;
import androidx.annotation.NonNull;
import androidx.annotation.RequiresApi;

@RequiresApi(21)
public abstract class initAdapterManager extends setScrollingTouchSlop {
    @NonNull
    public abstract setScrollingTouchSlop getMax();

    public void length(@NonNull getFullClassName getfullclassname) {
        super.length(getfullclassname);
        getMax().getMin((hasFixedSize) new hasFixedSize() {
            public final void length(@NonNull setHasFixedSize sethasfixedsize, int i) {
                initAdapterManager.this.getMax(i);
                if (i == Integer.MAX_VALUE) {
                    sethasfixedsize.length(this);
                }
            }
        });
        getMax().length(getfullclassname);
    }

    /* access modifiers changed from: protected */
    public final void getMax(@NonNull getFullClassName getfullclassname) {
        super.getMax(getfullclassname);
        getMax().getMax(getfullclassname);
    }

    public final void getMax(@NonNull getFullClassName getfullclassname, @NonNull CaptureRequest captureRequest) {
        super.getMax(getfullclassname, captureRequest);
        getMax().getMax(getfullclassname, captureRequest);
    }

    public final void setMax(@NonNull getFullClassName getfullclassname, @NonNull CaptureRequest captureRequest, @NonNull CaptureResult captureResult) {
        super.setMax(getfullclassname, captureRequest, captureResult);
        getMax().setMax(getfullclassname, captureRequest, captureResult);
    }

    public void getMin(@NonNull getFullClassName getfullclassname, @NonNull CaptureRequest captureRequest, @NonNull TotalCaptureResult totalCaptureResult) {
        super.getMin(getfullclassname, captureRequest, totalCaptureResult);
        getMax().getMin(getfullclassname, captureRequest, totalCaptureResult);
    }
}
