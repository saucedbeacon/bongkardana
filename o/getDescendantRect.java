package o;

import com.bumptech.glide.load.DataSource;
import com.bumptech.glide.load.engine.GlideException;

public interface getDescendantRect {
    Object isInside();

    void length(fireTrigger<?> firetrigger, DataSource dataSource);

    void setMax(GlideException glideException);
}
