package o;

import o.addAnimatorUpdateListener;

final class getDownloadType implements addAnimatorUpdateListener.getMax {
    private final generateFromJSON getMax;

    public getDownloadType(generateFromJSON generatefromjson) {
        this.getMax = generatefromjson;
    }

    public final Object processFacade(Object obj) {
        return ((PermissionModel) obj).getUserConfig(this.getMax);
    }
}
