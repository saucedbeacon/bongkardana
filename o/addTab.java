package o;

import android.graphics.Path;
import java.util.ArrayList;
import java.util.List;

public final class addTab {
    List<getImeOptions> setMin = new ArrayList();

    public final void getMin(Path path) {
        for (int size = this.setMin.size() - 1; size >= 0; size--) {
            setTitleMarginStart.getMax(path, this.setMin.get(size));
        }
    }
}
