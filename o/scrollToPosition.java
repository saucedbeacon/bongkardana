package o;

import com.google.common.net.HttpHeaders;
import com.mixpanel.android.java_websocket.drafts.Draft;
import com.mixpanel.android.java_websocket.exceptions.InvalidHandshakeException;

public final class scrollToPosition extends isLayoutRTL {
    public final Draft.HandshakeState getMax(computeScrollRange computescrollrange) throws InvalidHandshakeException {
        if (length((resolveIsInfinite) computescrollrange) == 13) {
            return Draft.HandshakeState.MATCHED;
        }
        return Draft.HandshakeState.NOT_MATCHED;
    }

    public final scrollVerticallyBy length(scrollVerticallyBy scrollverticallyby) {
        super.length(scrollverticallyby);
        scrollverticallyby.length(HttpHeaders.SEC_WEBSOCKET_VERSION, "13");
        return scrollverticallyby;
    }

    public final Draft getMax() {
        return new scrollToPosition();
    }
}
