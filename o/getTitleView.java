package o;

import java.util.concurrent.Callable;

public final class getTitleView implements RedDotDrawable {
    public static final getTitleView length = new getTitleView();

    final class core implements Callable {
        private final String getMin;
        private final getFrameMarginVertical$core length;

        public core(getFrameMarginVertical$core getframemarginvertical_core, String str) {
            this.length = getframemarginvertical_core;
            this.getMin = str;
        }

        public final Object call() {
            return this.length.lambda$setReadInterstitialBanner$21(this.getMin);
        }
    }

    public final Object apply(Object obj) {
        return itemClicked.toTransaction((isItemChecked) obj);
    }
}
