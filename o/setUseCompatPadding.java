package o;

public interface setUseCompatPadding {
    public static final setUseCompatPadding getMin = new setUseCompatPadding() {
        public final boolean length(int i) {
            int max = dispatchOnCancelled.setMax(i);
            if (i != max) {
                onCanceled oncanceled = new onCanceled(i, max, 1);
                onCancelLoad.setMax(1671254504, oncanceled);
                onCancelLoad.getMin(1671254504, oncanceled);
            }
            return true;
        }

        public final boolean getMin(put put, int i) {
            put.setMin((long) i);
            return true;
        }
    };

    boolean getMin(put put, int i);

    boolean length(int i);
}
