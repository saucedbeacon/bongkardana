package o;

import androidx.annotation.NonNull;
import java.util.List;

public interface isComputingLayout {
    @NonNull
    List<shouldDeferAccessibilityEvent> setMin(@NonNull List<shouldDeferAccessibilityEvent> list);
}
