package o;

public class NotificationCompat$StreamType {
    public static final getMax getMax = new length((byte) 0);
    private static volatile getMin length = null;

    public interface getMax {
    }

    public interface getMin {
    }

    private NotificationCompat$StreamType() {
    }

    public static void getMax() {
        equals();
    }

    public static void setMax() {
        equals();
    }

    public static void length() {
        equals();
    }

    public static void getMin() {
        equals();
    }

    public static boolean setMin() {
        equals();
        return false;
    }

    private static getMin equals() {
        if (length == null) {
            synchronized (NotificationCompat$StreamType.class) {
                if (length == null) {
                    length = new delete();
                }
            }
        }
        return length;
    }

    static final class length implements getMax {
        private length() {
        }

        /* synthetic */ length(byte b) {
            this();
        }
    }
}
