package o;

import android.content.Context;
import android.graphics.drawable.Drawable;
import android.widget.ImageView;
import com.bumptech.glide.Glide;
import o.setOnMenuWindowDismissListener;

public final class initWindowManagerLayoutParams extends getDismissAnimationId {
    public initWindowManagerLayoutParams(String str, setOnMenuWindowDismissListener.length length) {
        super(str, length);
    }

    public final void length(Context context, ImageView imageView) {
        updateMessageMenuItem<Drawable> min = ((updateCornerMarking) Glide.getMax(context)).setMax(getMax()).setMax(evaluate.setMin).setMax(setMax()).setMin(setMax());
        if (getMin() != null) {
            min.getMin((getTopSortedChildren<?>) getMin());
        }
        min.length(imageView);
    }
}
