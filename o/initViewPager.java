package o;

import java.util.List;
import o.setTextSpacing;

final class initViewPager implements RedDotManager {
    private final setTextSpacing.getMin getMax;

    public initViewPager(setTextSpacing.getMin getmin) {
        this.getMax = getmin;
    }

    public final void accept(Object obj) {
        this.getMax.length((List) obj);
    }
}
