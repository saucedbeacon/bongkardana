package o;

import org.yaml.snakeyaml.events.Event;
import org.yaml.snakeyaml.tokens.Token;

class getAutoLogActivities$Mean$Arithmetic implements getUploader {
    final /* synthetic */ getAutoLogActivities length;

    private getAutoLogActivities$Mean$Arithmetic(getAutoLogActivities getautologactivities) {
        this.length = getautologactivities;
    }

    /* synthetic */ getAutoLogActivities$Mean$Arithmetic(getAutoLogActivities getautologactivities, byte b) {
        this(getautologactivities);
    }

    public final Event setMax() {
        Token min = this.length.getMax.getMin();
        if (!this.length.getMax.getMax(Token.ID.Value, Token.ID.FlowEntry, Token.ID.FlowSequenceEnd)) {
            AbtestInfoGetter length2 = this.length.setMin;
            length2.setMax.add(new getAutoLogActivities$Grayscale$Algorithm(this.length, (byte) 0));
            return this.length.getMax(false, false);
        }
        getAutoLogActivities getautologactivities = this.length;
        getUploader unused = getautologactivities.isInside = new getAutoLogActivities$Grayscale$Algorithm(getautologactivities, (byte) 0);
        return getAutoLogActivities.setMin(min.toFloatRange);
    }
}
