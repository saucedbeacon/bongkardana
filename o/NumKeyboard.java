package o;

import java.lang.annotation.Annotation;
import java.util.Arrays;
import java.util.Collections;
import java.util.HashSet;
import java.util.Set;
import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;

public final class NumKeyboard extends setDynamicTextView {

    public final class KeyType extends setSendAccessiBilityEventListener {
        private static final Set<Class<? extends Annotation>> getMin = Collections.unmodifiableSet(new HashSet(Arrays.asList(new Class[]{BeforeClass.class, AfterClass.class, Before.class, After.class})));
    }
}
