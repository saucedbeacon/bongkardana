package o;

import javax.inject.Inject;
import o.TitleBarRightButtonView;

public class savePassword extends parseFailed<getPageUrl, setMax> {
    private setDefaultFontSize featureConfigRepository;

    public static class setMax {
    }

    @Inject
    public savePassword(appxLoadFailed appxloadfailed, getScheme getscheme, setDefaultFontSize setdefaultfontsize) {
        super(appxloadfailed, getscheme);
        this.featureConfigRepository = setdefaultfontsize;
    }

    /* access modifiers changed from: protected */
    public TitleBarRightButtonView.AnonymousClass1<getPageUrl> buildUseCaseObservable(setMax setmax) {
        return TitleBarRightButtonView.AnonymousClass1.zip(this.featureConfigRepository.getPromoCenterConfig(), this.featureConfigRepository.getPromoBannerSliderItemNewSizeEnabled(), overlayHorizontalScrollbar.length);
    }
}
