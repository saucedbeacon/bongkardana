package o;

import o.OnConfirmListener;

public final class appendDnsTime implements OnConfirmListener.getMax, getEditContent, clearText {
    public final void getMin(OnConfirmListener.length length) {
        length.setMax.add(new appendWsHsTime());
    }
}
