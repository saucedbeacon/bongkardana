package o;

import o.addAnimatorUpdateListener;

final class onWebViewResume implements addAnimatorUpdateListener.getMax {
    private final onAttachedToWebView setMax;

    public onWebViewResume(onAttachedToWebView onattachedtowebview) {
        this.setMax = onattachedtowebview;
    }

    public final Object processFacade(Object obj) {
        return ((onDetachedToWebView) obj).hasNewNotification(this.setMax);
    }
}
