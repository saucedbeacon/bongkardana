package o;

import android.graphics.Bitmap;
import androidx.annotation.Nullable;

interface getTargetPosition {
    int getMax(Bitmap bitmap);

    String getMax(int i, int i2, Bitmap.Config config);

    @Nullable
    Bitmap length();

    void length(Bitmap bitmap);

    @Nullable
    Bitmap setMax(int i, int i2, Bitmap.Config config);

    String setMin(Bitmap bitmap);
}
