package o;

import java.util.List;
import javax.inject.Inject;
import o.TitleBarRightButtonView;

public class zoomIn extends parseFailed<List<setDownloadListener>, Void> {
    private final setDefaultFontSize featureConfigRepository;

    @Inject
    public zoomIn(appxLoadFailed appxloadfailed, getScheme getscheme, setDefaultFontSize setdefaultfontsize) {
        super(appxloadfailed, getscheme);
        this.featureConfigRepository = setdefaultfontsize;
    }

    /* access modifiers changed from: protected */
    public TitleBarRightButtonView.AnonymousClass1<List<setDownloadListener>> buildUseCaseObservable(Void voidR) {
        return this.featureConfigRepository.getExpiryChoice();
    }
}
