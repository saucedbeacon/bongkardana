package o;

import androidx.annotation.NonNull;
import androidx.annotation.RequiresApi;
import java.util.Arrays;

@RequiresApi(21)
public final class removeRecyclerListener extends initAdapterManager {
    private final setScrollingTouchSlop length = new addRecyclerListener(Arrays.asList(new setScrollingTouchSlop[]{new setAdapterInternal(), new getAdapter(), new addOnChildAttachStateChangeListener()}));

    @NonNull
    public final setScrollingTouchSlop getMax() {
        return this.length;
    }
}
