package o;

import androidx.annotation.NonNull;
import androidx.annotation.RequiresApi;

@RequiresApi(21)
public abstract class setRecyclerListener extends setScrollingTouchSlop {
    /* access modifiers changed from: protected */
    public abstract void IsOverlapping(@NonNull getFullClassName getfullclassname);

    /* access modifiers changed from: protected */
    public abstract boolean equals(@NonNull getFullClassName getfullclassname);

    /* access modifiers changed from: protected */
    public abstract boolean toFloatRange(@NonNull getFullClassName getfullclassname);

    public final void length(@NonNull getFullClassName getfullclassname) {
        super.length(getfullclassname);
        boolean floatRange = toFloatRange(getfullclassname);
        if (!equals(getfullclassname) || floatRange) {
            getMax(Integer.MAX_VALUE);
        } else {
            IsOverlapping(getfullclassname);
        }
    }
}
