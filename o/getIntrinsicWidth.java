package o;

import androidx.annotation.MainThread;
import androidx.annotation.NonNull;
import androidx.fragment.app.Fragment;

public interface getIntrinsicWidth {
    @MainThread
    void getMax(@NonNull Fragment fragment);
}
