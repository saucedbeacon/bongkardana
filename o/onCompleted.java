package o;

import o.onNext;

final class onCompleted implements RedDotManager {
    private final onNext.length getMin;

    public onCompleted(onNext.length length) {
        this.getMin = length;
    }

    public final void accept(Object obj) {
        this.getMin.getMin(((Boolean) obj).booleanValue());
    }
}
