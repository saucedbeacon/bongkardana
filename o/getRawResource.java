package o;

import java.util.List;

final class getRawResource implements RedDotDrawable {
    private final setAppVersion getMin;

    public getRawResource(setAppVersion setappversion) {
        this.getMin = setappversion;
    }

    public final Object apply(Object obj) {
        return this.getMin.lambda$syncContact$1((List) obj);
    }
}
