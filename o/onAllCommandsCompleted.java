package o;

public final class onAllCommandsCompleted implements getBindingAdapter<ConstraintProxy$NetworkStateProxy> {
    public static void length(ConstraintProxy$NetworkStateProxy constraintProxy$NetworkStateProxy, Beacon beacon, GriverBottomPopupDialog griverBottomPopupDialog, MovableFrameLayout movableFrameLayout, loadUrl loadurl) {
        constraintProxy$NetworkStateProxy.length = beacon;
        constraintProxy$NetworkStateProxy.getMin = movableFrameLayout;
        constraintProxy$NetworkStateProxy.setMin = griverBottomPopupDialog;
        constraintProxy$NetworkStateProxy.getMax = loadurl;
    }
}
