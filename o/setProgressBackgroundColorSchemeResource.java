package o;

import o.onDelete;

public interface setProgressBackgroundColorSchemeResource {

    public interface setMax extends onDelete.getMin {
        void getMax();

        void length();
    }

    public interface setMin extends onDelete.length {
        void setMax(boolean z);
    }
}
