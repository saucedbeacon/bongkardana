package o;

import android.widget.ImageView;
import com.bumptech.glide.Glide;
import id.dana.R;
import id.dana.news.viewholders.NewsFullViewHolder;
import o.Popup;

public final class stopHCE implements Popup.getMax {
    private final NewsFullViewHolder length;

    public stopHCE(NewsFullViewHolder newsFullViewHolder) {
        this.length = newsFullViewHolder;
    }

    public final void setMin(String str, ImageView imageView) {
        ((updateCornerMarking) Glide.getMax(this.length.length)).setMax(str).setMax((int) R.drawable.square_rounded_skeleton).setMax(evaluate.setMin).setMin((int) R.drawable.square_rounded_skeleton).length(imageView);
    }
}
