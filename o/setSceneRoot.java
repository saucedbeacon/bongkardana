package o;

import id.dana.splitbill.model.SplitBillModel;
import javax.inject.Inject;

public final class setSceneRoot {
    final updateFootState getMin;
    final applySettingsToScheduledJob length;

    interface setMin {
        void setMax(SplitBillModel splitBillModel);

        void setMax(Throwable th);
    }

    @Inject
    setSceneRoot(applySettingsToScheduledJob applysettingstoscheduledjob, updateFootState updatefootstate) {
        this.length = applysettingstoscheduledjob;
        this.getMin = updatefootstate;
    }
}
