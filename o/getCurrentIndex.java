package o;

import java.util.concurrent.Callable;
import o.TitleBarRightButtonView;

final class getCurrentIndex implements Callable {
    public static final getCurrentIndex setMin = new getCurrentIndex();

    public final Object call() {
        return TitleBarRightButtonView.AnonymousClass1.create(getSelectedPage.length);
    }
}
