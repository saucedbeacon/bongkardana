package o;

import android.content.Context;
import android.os.Build;
import androidx.annotation.NonNull;
import androidx.work.NetworkType;

public final class setImageResource extends setImageDrawable<onTextContextMenuItem> {
    /* access modifiers changed from: package-private */
    public final /* bridge */ /* synthetic */ boolean getMin(@NonNull Object obj) {
        return getMin((onTextContextMenuItem) obj);
    }

    public setImageResource(Context context, getMinWidthMinor getminwidthminor) {
        super(AppCompatPopupWindow.setMax(context, getminwidthminor).getMin);
    }

    /* access modifiers changed from: package-private */
    public final boolean getMax(@NonNull getInternalPopup getinternalpopup) {
        return getinternalpopup.toFloatRange.length == NetworkType.CONNECTED;
    }

    private static boolean getMin(@NonNull onTextContextMenuItem ontextcontextmenuitem) {
        return Build.VERSION.SDK_INT >= 26 ? !ontextcontextmenuitem.getMax() || !ontextcontextmenuitem.setMax() : !ontextcontextmenuitem.getMax();
    }
}
