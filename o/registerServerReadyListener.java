package o;

import o.addAnimatorUpdateListener;

final class registerServerReadyListener implements addAnimatorUpdateListener.getMax {
    private final getClientChannel getMax;

    public registerServerReadyListener(getClientChannel getclientchannel) {
        this.getMax = getclientchannel;
    }

    public final Object processFacade(Object obj) {
        return ((getServerChannel) obj).withdrawInit(this.getMax);
    }
}
