package o;

import o.setLayoutResource;

public final class CustomTabsClient$2 implements setLayoutResource {
    public final getCardElevation getMax;

    public CustomTabsClient$2(getCardElevation getcardelevation) {
        this.getMax = getcardelevation;
    }

    public final getPreventCornerOverlap getMax(setLayoutResource.getMin getmin) {
        getTokenStore gettokenstore = (getTokenStore) getmin;
        getRadius getradius = gettokenstore.setMin;
        onBind onbind = gettokenstore.length;
        return gettokenstore.setMax(getradius, onbind, onbind.length(this.getMax, !getradius.setMax.equals("GET")), onbind.getMin());
    }
}
