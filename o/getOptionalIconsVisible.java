package o;

import androidx.sqlite.db.SupportSQLiteDatabase;
import o.setHeaderInternal;

final class getOptionalIconsVisible implements next {
    private final next getMax;
    private final setHeaderInternal.setMin getMin;

    public getOptionalIconsVisible(setHeaderInternal.setMin setmin, next next) {
        this.getMin = setmin;
        this.getMax = next;
    }

    public final Object length(Object obj) {
        return setHeaderInternal.setMin.setMax(this.getMin, this.getMax, (SupportSQLiteDatabase) obj);
    }
}
