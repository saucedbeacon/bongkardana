package o;

import o.getAutoLogActivities;
import org.yaml.snakeyaml.events.Event;
import org.yaml.snakeyaml.tokens.Token;

class getAutoLogActivities$FastBitmap$CoordinateSystem implements getUploader {
    final /* synthetic */ getAutoLogActivities setMax;

    private getAutoLogActivities$FastBitmap$CoordinateSystem(getAutoLogActivities getautologactivities) {
        this.setMax = getautologactivities;
    }

    /* synthetic */ getAutoLogActivities$FastBitmap$CoordinateSystem(getAutoLogActivities getautologactivities, byte b) {
        this(getautologactivities);
    }

    public final Event setMax() {
        Token min = this.setMax.getMax.getMin();
        AbtestInfoGetter min2 = this.setMax.length;
        min2.setMax.add(min.getMin);
        return new getAutoLogActivities.hashCode(true).setMax();
    }
}
