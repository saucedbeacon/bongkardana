package o;

public interface setLayoutResource {

    public interface getMin {
        getPreventCornerOverlap setMax(getRadius getradius);

        getRadius setMax();
    }

    getPreventCornerOverlap getMax(getMin getmin);
}
