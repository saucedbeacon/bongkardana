package o;

import android.net.Uri;

public final class getPhotoRect {
    private static getPhotoRect setMin;
    public Uri setMax;

    private getPhotoRect() {
    }

    public static synchronized getPhotoRect length() {
        getPhotoRect getphotorect;
        synchronized (getPhotoRect.class) {
            if (setMin == null) {
                setMin = new getPhotoRect();
            }
            getphotorect = setMin;
        }
        return getphotorect;
    }
}
