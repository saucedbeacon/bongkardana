package o;

import o.OnConfirmListener;
import o.getConnAllTime;

public final class getLinkLiveTime implements OnConfirmListener.getMax, getEditContent, clearText {
    public final void getMin(OnConfirmListener.length length) {
        length.length.add(new getConnAllTime.getMin());
    }
}
