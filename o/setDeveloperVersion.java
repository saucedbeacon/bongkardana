package o;

import o.addAnimatorUpdateListener;

final class setDeveloperVersion implements addAnimatorUpdateListener.getMax {
    private final setVhost setMax;

    public setDeveloperVersion(setVhost setvhost) {
        this.setMax = setvhost;
    }

    public final Object processFacade(Object obj) {
        return ((getVhost) obj).queryCheckUserInfo(this.setMax);
    }
}
