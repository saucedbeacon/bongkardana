package o;

import android.content.Context;
import androidx.annotation.NonNull;
import androidx.annotation.RestrictTo;

@RestrictTo({RestrictTo.Scope.LIBRARY_GROUP})
final class setButtonDrawable {
    private static final String getMin = setSupportBackgroundTintList.length("ConstraintsCmdHandler");
    final Context getMax;
    final int length;
    final setSupportButtonTintMode setMax;
    final getTextClassifier setMin;

    setButtonDrawable(@NonNull Context context, int i, @NonNull setSupportButtonTintMode setsupportbuttontintmode) {
        this.getMax = context;
        this.length = i;
        this.setMax = setsupportbuttontintmode;
        this.setMin = new getTextClassifier(this.getMax, setsupportbuttontintmode.getMax, (onDragEvent) null);
    }
}
