package o;

import androidx.annotation.NonNull;
import com.bumptech.glide.Registry;

public abstract class getStatusBarBackground implements setStatusBarBackgroundResource {
    public void getMin(@NonNull Registry registry) {
    }
}
