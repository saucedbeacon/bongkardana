package o;

import java.util.Queue;
import o.setInteractionEnabled;

abstract class getConstraintSetIds<T extends setInteractionEnabled> {
    final Queue<T> setMax = getDependents.length(20);

    /* access modifiers changed from: package-private */
    public abstract T getMin();

    getConstraintSetIds() {
    }
}
