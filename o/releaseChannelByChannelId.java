package o;

import o.BigDataChannelManager;
import o.addAnimatorUpdateListener;

final class releaseChannelByChannelId implements addAnimatorUpdateListener.getMax {
    private final BigDataChannelManager.AnonymousClass1 getMax;

    public releaseChannelByChannelId(BigDataChannelManager.AnonymousClass1 r1) {
        this.getMax = r1;
    }

    public final Object processFacade(Object obj) {
        return ((releaseAllChannel) obj).verify(this.getMax);
    }
}
