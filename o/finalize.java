package o;

import java.util.List;
import java.util.Map;

final class finalize implements RedDotDrawable {
    private final Map setMin;

    public finalize(Map map) {
        this.setMin = map;
    }

    public final Object apply(Object obj) {
        return poll.lambda$getReferralWidgets$2(this.setMin, (List) obj);
    }
}
