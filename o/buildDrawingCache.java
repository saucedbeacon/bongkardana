package o;

import o.TitleBarRightButtonView;

public final class buildDrawingCache implements getFrame {
    private final setFrame cardPreferences;

    public buildDrawingCache(setFrame setframe) {
        this.cardPreferences = setframe;
    }

    public final TitleBarRightButtonView.AnonymousClass1<Boolean> saveLastCardUsed(String str) {
        int length = str != null ? str.length() : 0;
        int max = dispatchOnCancelled.setMax(length);
        if (length != max) {
            onCanceled oncanceled = new onCanceled(length, max, 1);
            onCancelLoad.setMax(-2065181194, oncanceled);
            onCancelLoad.getMin(-2065181194, oncanceled);
        }
        return TitleBarRightButtonView.AnonymousClass1.just(this.cardPreferences.saveLastCardUsed(str));
    }

    public final TitleBarRightButtonView.AnonymousClass1<String> getDefaultCard() {
        return TitleBarRightButtonView.AnonymousClass1.just(this.cardPreferences.getDefaultCard());
    }
}
