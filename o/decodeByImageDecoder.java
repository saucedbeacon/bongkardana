package o;

import android.content.Context;
import javax.inject.Inject;

public final class decodeByImageDecoder {
    Context getMin;
    public IRedDotManager length = new IRedDotManager();

    @Inject
    public decodeByImageDecoder(Context context) {
        this.getMin = context;
    }
}
