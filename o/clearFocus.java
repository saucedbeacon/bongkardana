package o;

import android.graphics.Path;
import com.airbnb.lottie.LottieDrawable;
import com.airbnb.lottie.model.content.ShapeTrimPath;
import java.util.List;
import o.setInputType;

public final class clearFocus implements setImeOptions, setInputType.getMin {
    private addTab equals = new addTab();
    private final String getMax;
    private final LottieDrawable getMin;
    private final boolean length;
    private final setInputType<?, Path> setMax;
    private final Path setMin = new Path();
    private boolean toIntRange;

    public clearFocus(LottieDrawable lottieDrawable, onKeyPreIme onkeypreime, ensureImeVisible ensureimevisible) {
        this.getMax = ensureimevisible.setMin;
        this.length = ensureimevisible.getMin();
        this.getMin = lottieDrawable;
        setQueryHint setqueryhint = new setQueryHint(ensureimevisible.setMax.setMin);
        this.setMax = setqueryhint;
        if (setqueryhint != null) {
            onkeypreime.setMax.add(setqueryhint);
        }
        this.setMax.getMin.add(this);
    }

    public final void setMax(List<animateToTab> list, List<animateToTab> list2) {
        for (int i = 0; i < list.size(); i++) {
            animateToTab animatetotab = list.get(i);
            if (animatetotab instanceof getImeOptions) {
                getImeOptions getimeoptions = (getImeOptions) animatetotab;
                if (getimeoptions.setMin == ShapeTrimPath.Type.SIMULTANEOUSLY) {
                    this.equals.setMin.add(getimeoptions);
                    getimeoptions.length.add(this);
                }
            }
        }
    }

    public final Path getMax() {
        if (this.toIntRange) {
            return this.setMin;
        }
        this.setMin.reset();
        if (this.length) {
            this.toIntRange = true;
            return this.setMin;
        }
        this.setMin.set(this.setMax.equals());
        this.setMin.setFillType(Path.FillType.EVEN_ODD);
        this.equals.getMin(this.setMin);
        this.toIntRange = true;
        return this.setMin;
    }

    public final String setMin() {
        return this.getMax;
    }

    public final void setMax() {
        this.toIntRange = false;
        this.getMin.invalidateSelf();
    }
}
