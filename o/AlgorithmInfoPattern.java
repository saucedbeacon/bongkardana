package o;

public class AlgorithmInfoPattern extends isHasFace {
    public LogFactory ICustomTabsCallback;
    private LogFactory isInside;
    private AlgorithmInfoPattern toFloatRange;

    public AlgorithmInfoPattern() {
        this.getMin = -1;
    }

    public AlgorithmInfoPattern(int i) {
        this.getMin = i;
    }

    public AlgorithmInfoPattern(int i, LogFactory logFactory) {
        this(i);
        getMax(logFactory);
    }

    public AlgorithmInfoPattern(LogFactory logFactory, int i) {
        this(82, logFactory);
        setMin(i);
    }

    public final AlgorithmInfoPattern extraCallbackWithResult() {
        if (this.getMin == 121 || this.getMin == 122) {
            return this.toFloatRange;
        }
        throw getStyleId.setMin();
    }

    public final void setMin(AlgorithmInfoPattern algorithmInfoPattern) {
        if (this.getMin != 121 && this.getMin != 122) {
            throw getStyleId.setMin();
        } else if (algorithmInfoPattern == null) {
            throw getStyleId.setMin();
        } else if (this.toFloatRange == null) {
            this.toFloatRange = algorithmInfoPattern;
        } else {
            throw getStyleId.setMin();
        }
    }

    public final LogFactory onPostMessage() {
        if (this.getMin == 115) {
            return this.isInside;
        }
        throw getStyleId.setMin();
    }

    public final void equals(LogFactory logFactory) {
        if (this.getMin != 115) {
            throw getStyleId.setMin();
        } else if (logFactory.length() != 132) {
            throw getStyleId.setMin();
        } else if (this.isInside == null) {
            this.isInside = logFactory;
        } else {
            throw getStyleId.setMin();
        }
    }

    public final LogFactory onRelationshipValidationResult() {
        if (this.getMin == 82) {
            return this.isInside;
        }
        throw getStyleId.setMin();
    }

    public final void toIntRange(LogFactory logFactory) {
        if (this.getMin != 82) {
            throw getStyleId.setMin();
        } else if (logFactory.length() != 132) {
            throw getStyleId.setMin();
        } else if (this.isInside == null) {
            this.isInside = logFactory;
        } else {
            throw getStyleId.setMin();
        }
    }

    public final void length(AlgorithmInfoPattern algorithmInfoPattern) {
        if (this.getMin != 131) {
            throw getStyleId.setMin();
        } else if (algorithmInfoPattern == null) {
            throw getStyleId.setMin();
        } else if (this.toFloatRange == null) {
            this.toFloatRange = algorithmInfoPattern;
        } else {
            throw getStyleId.setMin();
        }
    }

    public final LogFactory onMessageChannelReady() {
        if (this.getMin == 133) {
            return this.isInside;
        }
        throw getStyleId.setMin();
    }

    public final void isInside(LogFactory logFactory) {
        if (this.getMin != 133) {
            throw getStyleId.setMin();
        } else if (logFactory.length() != 132) {
            throw getStyleId.setMin();
        } else if (this.isInside == null) {
            this.isInside = logFactory;
        } else {
            throw getStyleId.setMin();
        }
    }
}
