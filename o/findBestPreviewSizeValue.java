package o;

public class findBestPreviewSizeValue {
    public String getMax;
    public String getMin;
    public String length;
    public int setMin;

    public static class getMin {
        String getMax;
        String getMin;
        int setMax;
        String setMin;
    }

    /* synthetic */ findBestPreviewSizeValue(getMin getmin, byte b) {
        this(getmin);
    }

    private findBestPreviewSizeValue(getMin getmin) {
        this.getMin = getmin.getMax;
        this.getMax = getmin.getMin;
        this.length = getmin.setMin;
        this.setMin = getmin.setMax;
    }
}
