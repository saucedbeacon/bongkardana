package o;

import androidx.annotation.NonNull;
import androidx.annotation.RestrictTo;
import java.util.List;

public abstract class getSupportBackgroundTintList {
    private static final String getMax = setSupportBackgroundTintList.length("InputMerger");

    @NonNull
    public abstract updateAppearance getMax(@NonNull List<updateAppearance> list);

    @RestrictTo({RestrictTo.Scope.LIBRARY_GROUP})
    public static getSupportBackgroundTintList getMax(String str) {
        try {
            return (getSupportBackgroundTintList) Class.forName(str).newInstance();
        } catch (Exception e) {
            setSupportBackgroundTintList.getMax();
            new Throwable[1][0] = e;
            return null;
        }
    }
}
