package o;

import o.IpcChannelManager;
import o.addAnimatorUpdateListener;

final class unRegisterServerReadyListener implements addAnimatorUpdateListener.getMax {
    private final IpcChannelManager.ClientListener length;

    public unRegisterServerReadyListener(IpcChannelManager.ClientListener clientListener) {
        this.length = clientListener;
    }

    public final Object processFacade(Object obj) {
        return ((getServerChannel) obj).transferSubmit(this.length);
    }
}
