package o;

import android.content.Context;
import androidx.annotation.NonNull;
import androidx.annotation.RestrictTo;

@RestrictTo({RestrictTo.Scope.LIBRARY_GROUP})
public final class AppCompatCheckedTextView implements getAutoSizeMaxTextSize {
    private static final String setMax = setSupportBackgroundTintList.length("SystemAlarmScheduler");
    private final Context getMax;

    public final boolean getMax() {
        return true;
    }

    public AppCompatCheckedTextView(@NonNull Context context) {
        this.getMax = context.getApplicationContext();
    }

    public final void getMin(@NonNull getInternalPopup... getinternalpopupArr) {
        for (getInternalPopup getinternalpopup : getinternalpopupArr) {
            setSupportBackgroundTintList.getMax();
            String str = getinternalpopup.getMax;
            this.getMax.startService(setSupportButtonTintList.getMax(this.getMax, getinternalpopup.getMax));
        }
    }

    public final void setMax(@NonNull String str) {
        this.getMax.startService(setSupportButtonTintList.length(this.getMax, str));
    }
}
