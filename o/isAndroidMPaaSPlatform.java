package o;

public final class isAndroidMPaaSPlatform extends onUserConfirm {
    private final getAlphabet setMax = new getAlphabet();

    public final boolean length() {
        return true;
    }

    public final boolean setMax(SCNetworkAsyncTaskExecutorFactory sCNetworkAsyncTaskExecutorFactory) {
        return true;
    }

    public final setRsaPublicKey setMax(setEncryptRandomStringAndType setencryptrandomstringandtype) {
        int max = setencryptrandomstringandtype.setMax();
        if (!setMin(setencryptrandomstringandtype, max)) {
            return null;
        }
        int length = setencryptrandomstringandtype.length() + setencryptrandomstringandtype.toFloatRange() + 1;
        if (SSLExtensions.setMin(setencryptrandomstringandtype.getMax(), max + 1)) {
            length++;
        }
        return new isAndroidPlatform(-1, length, false);
    }

    /* access modifiers changed from: private */
    public static boolean setMin(setEncryptRandomStringAndType setencryptrandomstringandtype, int i) {
        CharSequence max = setencryptrandomstringandtype.getMax();
        return setencryptrandomstringandtype.toFloatRange() < SSLExtensions.setMax && i < max.length() && max.charAt(i) == '>';
    }

    public static class getMin extends EncryptRandomType {
        public final setNeedConfirmButton length(setEncryptRandomStringAndType setencryptrandomstringandtype, SafeInputContext safeInputContext) {
            int max = setencryptrandomstringandtype.setMax();
            if (!isAndroidMPaaSPlatform.setMin(setencryptrandomstringandtype, max)) {
                return null;
            }
            int length = setencryptrandomstringandtype.length() + setencryptrandomstringandtype.toFloatRange() + 1;
            if (SSLExtensions.setMin(setencryptrandomstringandtype.getMax(), max + 1)) {
                length++;
            }
            return new createAndroidMPaaSLogImpl(new isAndroidMPaaSPlatform()).getMax(length);
        }
    }

    public final /* bridge */ /* synthetic */ SCNetworkAsyncTaskExecutorFactory getMax() {
        return this.setMax;
    }
}
