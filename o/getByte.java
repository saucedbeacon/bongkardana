package o;

import o.addAnimatorUpdateListener;

final class getByte implements addAnimatorUpdateListener.getMax {
    private final Pool getMin;

    public getByte(Pool pool) {
        this.getMin = pool;
    }

    public final Object processFacade(Object obj) {
        return ((putBoolean) obj).redeemQuest(this.getMin);
    }
}
