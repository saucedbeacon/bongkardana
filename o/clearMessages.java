package o;

import o.OnConfirmListener;
import o.setMessagesBytes;

public final class clearMessages implements OnConfirmListener.getMax {
    public final void getMin(OnConfirmListener.length length) {
        length.length.add(new setMessagesBytes.setMax());
    }
}
