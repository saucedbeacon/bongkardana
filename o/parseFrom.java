package o;

import android.text.TextUtils;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

public final class parseFrom extends getAppIDBytes {
    @Nullable
    public final Object getMax(@NonNull appendFlushLog appendflushlog, @NonNull getLogEventType getlogeventtype) {
        String str = getlogeventtype.getMax().get("href");
        if (!TextUtils.isEmpty(str)) {
            return appendflushlog.isInside.setMin(appendflushlog.setMax, appendflushlog.getMin.setMax(str), appendflushlog.getMax);
        }
        return null;
    }
}
