package o;

import o.addAnimatorUpdateListener;

final class doubleDefault implements addAnimatorUpdateListener.getMax {
    private final stringDefault setMax;

    public doubleDefault(stringDefault stringdefault) {
        this.setMax = stringdefault;
    }

    public final Object processFacade(Object obj) {
        return ((BindingCallback) obj).getNearbyMeByRadius(this.setMax);
    }
}
