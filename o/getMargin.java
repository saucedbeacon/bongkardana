package o;

import android.graphics.Bitmap;
import androidx.annotation.NonNull;
import java.security.MessageDigest;

public final class getMargin extends setType {
    private static final byte[] setMax = "com.bumptech.glide.load.resource.bitmap.CenterInside".getBytes(setMin);

    /* access modifiers changed from: protected */
    public final Bitmap setMax(@NonNull rebuildScene rebuildscene, @NonNull Bitmap bitmap, int i, int i2) {
        return setTag.length(rebuildscene, bitmap, i, i2);
    }

    public final boolean equals(Object obj) {
        return obj instanceof getMargin;
    }

    public final int hashCode() {
        return "com.bumptech.glide.load.resource.bitmap.CenterInside".hashCode();
    }

    public final void setMax(@NonNull MessageDigest messageDigest) {
        messageDigest.update(setMax);
    }
}
