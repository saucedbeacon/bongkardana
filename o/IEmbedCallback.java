package o;

import o.addAnimatorUpdateListener;

final class IEmbedCallback implements addAnimatorUpdateListener.getMax {
    private final onParamChanged length;

    public IEmbedCallback(onParamChanged onparamchanged) {
        this.length = onparamchanged;
    }

    public final Object processFacade(Object obj) {
        return ((onDetachedToWebView) obj).fetchInboxNotification(this.length);
    }
}
