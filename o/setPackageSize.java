package o;

import id.dana.data.user.source.network.request.UserInfoRequest;
import o.addAnimatorUpdateListener;

final class setPackageSize implements addAnimatorUpdateListener.getMax {
    private final UserInfoRequest setMax;

    public setPackageSize(UserInfoRequest userInfoRequest) {
        this.setMax = userInfoRequest;
    }

    public final Object processFacade(Object obj) {
        return ((getVhost) obj).getUserInfo(this.setMax);
    }
}
