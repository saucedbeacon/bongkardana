package o;

import androidx.annotation.NonNull;
import androidx.recyclerview.widget.RecyclerView;
import androidx.recyclerview.widget.RecyclerView.valueOf;
import com.yarolegovich.discretescrollview.DiscreteScrollView;

public final class onSmoothScrollerStopped<T extends RecyclerView.valueOf> implements DiscreteScrollView.getMax<T> {
    private DiscreteScrollView.length<T> getMin;

    public final void setMin(int i) {
        int max = dispatchOnCancelled.setMax(i);
        if (i != max) {
            onCanceled oncanceled = new onCanceled(i, max, 1);
            onCancelLoad.setMax(951130132, oncanceled);
            onCancelLoad.getMin(951130132, oncanceled);
        }
    }

    public onSmoothScrollerStopped(@NonNull DiscreteScrollView.length<T> length) {
        this.getMin = length;
    }

    public final boolean equals(Object obj) {
        if (obj instanceof onSmoothScrollerStopped) {
            return this.getMin.equals(((onSmoothScrollerStopped) obj).getMin);
        }
        return super.equals(obj);
    }
}
