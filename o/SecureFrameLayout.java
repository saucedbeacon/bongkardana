package o;

import java.util.Arrays;
import java.util.List;
import o.QwertyKeyboard;

public final class SecureFrameLayout implements QwertyKeyboard.ShiftType {
    private static final List<getMin<?>> setMin = Arrays.asList(new getMin[]{new setMax((byte) 0), new length((byte) 0), new getMax((byte) 0)});

    public final class SendAccessiBilityEventListener {
        public short IsOverlapping;
        public short getMax;
        public short getMin;
        public int isInside;
        public short length;
        public boolean setMax = false;
        public short setMin;
        public short toIntRange;

        public SendAccessiBilityEventListener(short s, short s2, short s3) {
            this.length = s;
            this.getMax = s2;
            this.getMin = s3;
        }
    }

    static abstract class getMin<T> {
        private static final SecureEditText setMax = new SecureEditText();

        private getMin() {
        }

        /* synthetic */ getMin(byte b) {
            this();
        }
    }

    static class setMax extends getMin<Object> {
        private setMax() {
            super((byte) 0);
        }

        /* synthetic */ setMax(byte b) {
            this();
        }
    }

    static class length extends getMin<Object> {
        private length() {
            super((byte) 0);
        }

        /* synthetic */ length(byte b) {
            this();
        }
    }

    static class getMax extends getMin<Object> {
        private getMax() {
            super((byte) 0);
        }

        /* synthetic */ getMax(byte b) {
            this();
        }
    }
}
