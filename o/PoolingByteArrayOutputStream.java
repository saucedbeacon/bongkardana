package o;

import java.util.Map;

final class PoolingByteArrayOutputStream implements RedDotDrawable {
    private final poll getMax;
    private final Map setMax;

    public PoolingByteArrayOutputStream(poll poll, Map map) {
        this.getMax = poll;
        this.setMax = map;
    }

    public final Object apply(Object obj) {
        return this.getMax.lambda$getReferralWidgets$1(this.setMax, (Map.Entry) obj);
    }
}
