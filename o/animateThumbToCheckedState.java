package o;

import com.airbnb.lottie.parser.moshi.JsonReader;
import java.io.IOException;

final class animateThumbToCheckedState {
    private static JsonReader.setMax setMax = JsonReader.setMax.getMin("nm", "c", "o", "tr", "hd");

    static setThreshold setMax(JsonReader jsonReader, setVerticalGravity setverticalgravity) throws IOException {
        String str = null;
        onTextFocusChanged ontextfocuschanged = null;
        onTextFocusChanged ontextfocuschanged2 = null;
        forceSuggestionQuery forcesuggestionquery = null;
        boolean z = false;
        while (jsonReader.getMin()) {
            int min = jsonReader.setMin(setMax);
            if (min == 0) {
                str = jsonReader.toIntRange();
            } else if (min == 1) {
                ontextfocuschanged = new onTextFocusChanged(getSplitTrack.setMin(jsonReader, setverticalgravity, 1.0f, getThumbDrawable.getMax, false));
            } else if (min == 2) {
                ontextfocuschanged2 = new onTextFocusChanged(getSplitTrack.setMin(jsonReader, setverticalgravity, 1.0f, getThumbDrawable.getMax, false));
            } else if (min == 3) {
                forcesuggestionquery = setTrackTintMode.setMin(jsonReader, setverticalgravity);
            } else if (min != 4) {
                jsonReader.FastBitmap$CoordinateSystem();
            } else {
                z = jsonReader.equals();
            }
        }
        return new setThreshold(str, ontextfocuschanged, ontextfocuschanged2, forcesuggestionquery, z);
    }
}
