package o;

import android.view.View;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import o.removeMenuPresenter;

public final class addMenuPresenter {
    public static void getMax(@NonNull View view, @Nullable ListMenuItemView listMenuItemView) {
        view.setTag(removeMenuPresenter.getMin.length, listMenuItemView);
    }
}
