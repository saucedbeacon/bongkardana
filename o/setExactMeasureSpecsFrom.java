package o;

import androidx.annotation.NonNull;
import java.util.Map;
import o.getRowCountForAccessibility;

public final class setExactMeasureSpecsFrom implements getRowCountForAccessibility.getMax {
    public final void clearStatesNotIn(@NonNull Map<Object, isViewInvalid> map, @NonNull isItemRemoved isitemremoved) {
        map.keySet().retainAll(isitemremoved.getNewState());
    }
}
