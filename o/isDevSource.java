package o;

final class isDevSource implements RedDotDrawable {
    private final getOrigin getMax;
    private final String setMax;

    public isDevSource(getOrigin getorigin, String str) {
        this.getMax = getorigin;
        this.setMax = str;
    }

    public final Object apply(Object obj) {
        return this.getMax.lambda$isUserConfigSyncEnable$6(this.setMax, (readToArray) obj);
    }
}
