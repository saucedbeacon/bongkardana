package o;

import android.graphics.Canvas;
import android.graphics.Path;

public abstract class onGetLayoutInflater extends getShowsDialog {
    private Path getMax = new Path();

    public onGetLayoutInflater(setDrawerShadow setdrawershadow, setTargetFragment settargetfragment) {
        super(setdrawershadow, settargetfragment);
    }

    /* access modifiers changed from: protected */
    public final void getMin(Canvas canvas, float f, float f2, EmojiEditText emojiEditText) {
        this.IsOverlapping.setColor(emojiEditText.getMin());
        this.IsOverlapping.setStrokeWidth(emojiEditText.validateRelationship());
        this.IsOverlapping.setPathEffect(emojiEditText.ICustomTabsService$Default());
        if (emojiEditText.requestPostMessageChannel()) {
            this.getMax.reset();
            this.getMax.moveTo(f, this.invokeSuspend.toIntRange());
            this.getMax.lineTo(f, this.invokeSuspend.toFloatRange());
            canvas.drawPath(this.getMax, this.IsOverlapping);
        }
        if (emojiEditText.requestPostMessageChannelWithExtras()) {
            this.getMax.reset();
            this.getMax.moveTo(this.invokeSuspend.isInside(), f2);
            this.getMax.lineTo(this.invokeSuspend.equals(), f2);
            canvas.drawPath(this.getMax, this.IsOverlapping);
        }
    }
}
