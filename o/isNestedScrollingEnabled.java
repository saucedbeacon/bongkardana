package o;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

public final class isNestedScrollingEnabled {
    final Map<String, Object> getMax = new HashMap();
    final Set<String> getMin = new HashSet();

    public final void length(List<isStopped> list) {
        if (list != null) {
            if (list.size() <= 0) {
                this.getMin.clear();
            } else {
                list.get(0);
                throw new RuntimeException("Trying to record previous render data for component that doesn't support it");
            }
        }
    }
}
