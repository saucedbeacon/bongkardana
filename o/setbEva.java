package o;

import o.R;
import org.yaml.snakeyaml.DumperOptions;
import org.yaml.snakeyaml.error.YAMLException;

public class setbEva {
    private String equals;
    protected final notifyClientEvent getMax;
    protected DumperOptions getMin;
    protected R.bool length;
    protected setReleaseType setMax;
    protected getEvVer setMin;

    public setbEva() {
        this(new ICRpcService(), new setReleaseType(), new DumperOptions(), new getEvVer(), new notifyClientEvent());
    }

    private setbEva(R.bool bool, setReleaseType setreleasetype, DumperOptions dumperOptions, getEvVer getevver, notifyClientEvent notifyclientevent) {
        if (!bool.getMin()) {
            if (setreleasetype.toFloatRange == null) {
                setreleasetype.toFloatRange = new refreshSessionID();
            }
            bool.getMax(setreleasetype.toFloatRange);
        } else if (!setreleasetype.setMin()) {
            if (bool.toIntRange == null) {
                bool.toIntRange = new refreshSessionID();
            }
            setreleasetype.setMax(bool.toIntRange);
        }
        this.length = bool;
        bool.IsOverlapping = getevver.setMax();
        this.length.isInside = getevver.getMax();
        if (dumperOptions.setMax() || dumperOptions.length > dumperOptions.getMin) {
            setreleasetype.getMax(dumperOptions.getMax);
            setreleasetype.setMax(dumperOptions.setMin);
            if (setreleasetype.toFloatRange == null) {
                setreleasetype.toFloatRange = new refreshSessionID();
            }
            refreshSessionID refreshsessionid = setreleasetype.toFloatRange;
            boolean length2 = dumperOptions.length();
            if (refreshsessionid.setMax != length2) {
                refreshsessionid.setMax = length2;
                refreshsessionid.getMax.clear();
            }
            setreleasetype.length(dumperOptions.setMax);
            this.setMax = setreleasetype;
            this.getMin = dumperOptions;
            this.setMin = getevver;
            this.getMax = notifyclientevent;
            StringBuilder sb = new StringBuilder("Yaml:");
            sb.append(System.identityHashCode(this));
            this.equals = sb.toString();
            return;
        }
        throw new YAMLException("Indicator indent must be smaller then indent.");
    }

    public final Object length(addAutoLogActivity addautologactivity, Class<?> cls) {
        this.length.getMin(new getFeVer(new getAutoLogActivities(addautologactivity), this.getMax, this.setMin));
        return this.length.getMin(cls);
    }

    public String toString() {
        return this.equals;
    }
}
