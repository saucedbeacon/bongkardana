package o;

import o.addAnimatorUpdateListener;

final class stripAnchor implements addAnimatorUpdateListener.getMax {
    private final registerClientListener getMin;

    public stripAnchor(registerClientListener registerclientlistener) {
        this.getMin = registerclientlistener;
    }

    public final Object processFacade(Object obj) {
        return ((getServerChannel) obj).init(this.getMin);
    }
}
