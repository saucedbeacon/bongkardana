package o;

import java.util.HashSet;
import java.util.Set;

public final class setOnStateDescriptionOnRAndAbove implements getGravity {
    private static final Set<String> setMin = new HashSet();

    public final void getMin(String str) {
        if (!setMin.contains(str)) {
            setMin.add(str);
        }
    }

    public final void length() {
        boolean z = setChildFrame.length;
    }

    public final void setMax() {
        boolean z = setChildFrame.length;
    }

    public final void length(String str) {
        if (!setMin.contains(str)) {
            setMin.add(str);
        }
    }
}
