package o;

import java.util.List;

final class checkMsgIsValid implements RedDotDrawable {
    private final getRemoteDebugUrlForDebug getMin;
    private final Integer length;

    public checkMsgIsValid(getRemoteDebugUrlForDebug getremotedebugurlfordebug, Integer num) {
        this.getMin = getremotedebugurlfordebug;
        this.length = num;
    }

    public final Object apply(Object obj) {
        return this.getMin.lambda$monitorPinningMode$2(this.length, (List) obj);
    }
}
