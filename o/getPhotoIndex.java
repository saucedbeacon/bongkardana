package o;

import id.dana.sendmoney.view.ExpireTimeView;
import java.util.List;
import o.onExecuted;

public final class getPhotoIndex implements onExecuted.length {
    private final ExpireTimeView length;

    public getPhotoIndex(ExpireTimeView expireTimeView) {
        this.length = expireTimeView;
    }

    public final void setMax(List list) {
        ExpireTimeView.length(this.length, list);
    }
}
