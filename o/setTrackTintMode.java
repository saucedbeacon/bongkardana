package o;

import android.graphics.PointF;
import android.view.animation.Interpolator;
import com.airbnb.lottie.parser.moshi.JsonReader;
import com.alibaba.ariver.kernel.RVParams;
import id.dana.data.constant.BranchLinkConstant;
import java.io.IOException;
import java.util.List;
import o.SearchView;

public final class setTrackTintMode {
    private static final JsonReader.setMax setMax = JsonReader.setMax.getMin("k");
    private static final JsonReader.setMax setMin = JsonReader.setMax.getMin("a", "p", "s", "rz", BranchLinkConstant.PayloadKey.REFERRAL, "o", RVParams.SHOW_OPTION_MENU, "eo", "sk", "sa");

    public static forceSuggestionQuery setMin(JsonReader jsonReader, setVerticalGravity setverticalgravity) throws IOException {
        onTextFocusChanged ontextfocuschanged;
        JsonReader jsonReader2 = jsonReader;
        setVerticalGravity setverticalgravity2 = setverticalgravity;
        boolean z = false;
        boolean z2 = jsonReader.IsOverlapping() == JsonReader.Token.BEGIN_OBJECT;
        if (z2) {
            jsonReader.setMin();
        }
        onTextFocusChanged ontextfocuschanged2 = null;
        onVoiceClicked onvoiceclicked = null;
        SearchView.SavedState<PointF, PointF> savedState = null;
        onActionViewCollapsed onactionviewcollapsed = null;
        onTextFocusChanged ontextfocuschanged3 = null;
        onTextFocusChanged ontextfocuschanged4 = null;
        onCloseClicked oncloseclicked = null;
        onTextFocusChanged ontextfocuschanged5 = null;
        onTextFocusChanged ontextfocuschanged6 = null;
        while (jsonReader.getMin()) {
            switch (jsonReader2.setMin(setMin)) {
                case 0:
                    jsonReader.setMin();
                    while (jsonReader.getMin()) {
                        if (jsonReader2.setMin(setMax) != 0) {
                            jsonReader.isInside();
                            jsonReader.FastBitmap$CoordinateSystem();
                        } else {
                            onvoiceclicked = getTrackTintList.setMin(jsonReader, setverticalgravity);
                        }
                    }
                    jsonReader.setMax();
                    break;
                case 1:
                    savedState = getTrackTintList.setMax(jsonReader, setverticalgravity);
                    break;
                case 2:
                    onactionviewcollapsed = new onActionViewCollapsed((List<setTitleMarginTop<getLogo>>) getSplitTrack.setMin(jsonReader2, setverticalgravity2, 1.0f, stopDrag.setMin, false));
                    break;
                case 3:
                    setTitleMargin.setMax("Lottie doesn't support 3D layers.");
                    setverticalgravity2.length.add("Lottie doesn't support 3D layers.");
                    break;
                case 4:
                    break;
                case 5:
                    oncloseclicked = new onCloseClicked(getSplitTrack.setMin(jsonReader2, setverticalgravity2, 1.0f, applyThumbTint.length, z));
                    continue;
                case 6:
                    ontextfocuschanged5 = new onTextFocusChanged(getSplitTrack.setMin(jsonReader2, setverticalgravity2, 1.0f, getThumbDrawable.getMax, z));
                    continue;
                case 7:
                    ontextfocuschanged6 = new onTextFocusChanged(getSplitTrack.setMin(jsonReader2, setverticalgravity2, 1.0f, getThumbDrawable.getMax, z));
                    continue;
                case 8:
                    ontextfocuschanged3 = new onTextFocusChanged(getSplitTrack.setMin(jsonReader2, setverticalgravity2, 1.0f, getThumbDrawable.getMax, z));
                    continue;
                case 9:
                    ontextfocuschanged4 = new onTextFocusChanged(getSplitTrack.setMin(jsonReader2, setverticalgravity2, 1.0f, getThumbDrawable.getMax, z));
                    continue;
                default:
                    jsonReader.isInside();
                    jsonReader.FastBitmap$CoordinateSystem();
                    break;
            }
            onTextFocusChanged ontextfocuschanged7 = new onTextFocusChanged(getSplitTrack.setMin(jsonReader2, setverticalgravity2, 1.0f, getThumbDrawable.getMax, z));
            if (ontextfocuschanged7.setMin().isEmpty()) {
                List min = ontextfocuschanged7.setMin();
                setTitleMarginTop settitlemargintop = r1;
                ontextfocuschanged = ontextfocuschanged7;
                setTitleMarginTop settitlemargintop2 = new setTitleMarginTop(setverticalgravity, Float.valueOf(0.0f), Float.valueOf(0.0f), (Interpolator) null, 0.0f, Float.valueOf(setverticalgravity2.toDoubleRange));
                min.add(settitlemargintop);
            } else {
                ontextfocuschanged = ontextfocuschanged7;
                if (((setTitleMarginTop) ontextfocuschanged.setMin().get(0)).setMax == null) {
                    ontextfocuschanged.setMin().set(0, new setTitleMarginTop(setverticalgravity, Float.valueOf(0.0f), Float.valueOf(0.0f), (Interpolator) null, 0.0f, Float.valueOf(setverticalgravity2.toDoubleRange)));
                    ontextfocuschanged2 = ontextfocuschanged;
                    z = false;
                }
            }
            ontextfocuschanged2 = ontextfocuschanged;
            z = false;
        }
        if (z2) {
            jsonReader.setMax();
        }
        onVoiceClicked onvoiceclicked2 = length(onvoiceclicked) ? null : onvoiceclicked;
        SearchView.SavedState<PointF, PointF> savedState2 = setMin(savedState) ? null : savedState;
        onTextFocusChanged ontextfocuschanged8 = getMin(ontextfocuschanged2) ? null : ontextfocuschanged2;
        if (getMax(onactionviewcollapsed)) {
            onactionviewcollapsed = null;
        }
        return new forceSuggestionQuery(onvoiceclicked2, savedState2, onactionviewcollapsed, ontextfocuschanged8, oncloseclicked, ontextfocuschanged5, ontextfocuschanged6, length(ontextfocuschanged3) ? null : ontextfocuschanged3, getMax(ontextfocuschanged4) ? null : ontextfocuschanged4);
    }

    private static boolean length(onVoiceClicked onvoiceclicked) {
        if (onvoiceclicked != null) {
            return onvoiceclicked.getMin() && ((PointF) onvoiceclicked.setMin().get(0).setMax).equals(0.0f, 0.0f);
        }
        return true;
    }

    private static boolean setMin(SearchView.SavedState<PointF, PointF> savedState) {
        if (savedState == null) {
            return true;
        }
        if ((savedState instanceof onActionViewExpanded) || !savedState.getMin() || !((PointF) savedState.setMin().get(0).setMax).equals(0.0f, 0.0f)) {
            return false;
        }
        return true;
    }

    private static boolean getMin(onTextFocusChanged ontextfocuschanged) {
        if (ontextfocuschanged != null) {
            return ontextfocuschanged.getMin() && ((Float) ((setTitleMarginTop) ontextfocuschanged.setMin().get(0)).setMax).floatValue() == 0.0f;
        }
        return true;
    }

    private static boolean getMax(onActionViewCollapsed onactionviewcollapsed) {
        if (onactionviewcollapsed != null) {
            return onactionviewcollapsed.getMin() && ((getLogo) ((setTitleMarginTop) onactionviewcollapsed.setMin().get(0)).setMax).length();
        }
        return true;
    }

    private static boolean length(onTextFocusChanged ontextfocuschanged) {
        if (ontextfocuschanged != null) {
            return ontextfocuschanged.getMin() && ((Float) ((setTitleMarginTop) ontextfocuschanged.setMin().get(0)).setMax).floatValue() == 0.0f;
        }
        return true;
    }

    private static boolean getMax(onTextFocusChanged ontextfocuschanged) {
        if (ontextfocuschanged != null) {
            return ontextfocuschanged.getMin() && ((Float) ((setTitleMarginTop) ontextfocuschanged.setMin().get(0)).setMax).floatValue() == 0.0f;
        }
        return true;
    }
}
