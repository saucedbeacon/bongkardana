package o;

import androidx.recyclerview.widget.RecyclerView;
import o.requestChildRectangleOnScreen;

public class getScrimColor implements onDetachedFromLayoutParams {
    public final RecyclerView getMax;
    private final boolean getMin;
    public final RecyclerView.Adapter length;
    private final getScrimOpacity setMax;

    public /* synthetic */ getScrimColor(getMax getmax, byte b) {
        this(getmax);
    }

    private getScrimColor(getMax getmax) {
        this.getMax = getmax.setMax;
        this.length = getmax.length;
        getScrimOpacity getscrimopacity = new getScrimOpacity();
        this.setMax = getscrimopacity;
        getscrimopacity.setMax = getmax.setMin;
        this.setMax.getMax = getmax.getMax;
        this.setMax.getMin = getmax.getMin;
        this.setMax.setMin = getmax.toFloatRange;
        this.setMax.isInside = getmax.toIntRange;
        this.setMax.length = getmax.IsOverlapping;
        this.getMin = getmax.equals;
    }

    public final void getMin() {
        this.getMax.setAdapter(this.setMax);
        if (!this.getMax.isComputingLayout() && this.getMin) {
            this.getMax.setLayoutFrozen(true);
        }
    }

    public final void length() {
        this.getMax.setAdapter(this.length);
    }

    public static class getMax {
        public int IsOverlapping = 1000;
        /* access modifiers changed from: private */
        public boolean equals = true;
        public int getMax = requestChildRectangleOnScreen.setMin.layout_default_item_skeleton;
        public boolean getMin = true;
        public RecyclerView.Adapter length;
        public final RecyclerView setMax;
        public int setMin = 10;
        public int toFloatRange;
        public int toIntRange = 20;

        public getMax(RecyclerView recyclerView) {
            this.setMax = recyclerView;
            this.toFloatRange = IntRange.setMax(recyclerView.getContext(), requestChildRectangleOnScreen.getMin.getMin);
        }
    }
}
