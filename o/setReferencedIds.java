package o;

import android.graphics.Bitmap;
import android.graphics.ImageDecoder;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RequiresApi;
import java.io.IOException;
import java.io.InputStream;

@RequiresApi(api = 28)
public final class setReferencedIds implements setElevation<InputStream, Bitmap> {
    private final MotionTelltales getMin = new MotionTelltales();

    public final /* bridge */ /* synthetic */ boolean length(@NonNull Object obj, @NonNull MotionHelper motionHelper) throws IOException {
        return true;
    }

    @Nullable
    public final /* synthetic */ fireTrigger getMax(@NonNull Object obj, int i, int i2, @NonNull MotionHelper motionHelper) throws IOException {
        return this.getMin.getMax(ImageDecoder.createSource(setInsetOffsetY.length((InputStream) obj)), i, i2, motionHelper);
    }
}
