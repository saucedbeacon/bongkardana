package o;

final class hasSignature implements RedDotDrawable {
    private final isRemoteCallExtension getMin;
    private final GriverFullLinkStageMonitor length;

    public hasSignature(isRemoteCallExtension isremotecallextension, GriverFullLinkStageMonitor griverFullLinkStageMonitor) {
        this.getMin = isremotecallextension;
        this.length = griverFullLinkStageMonitor;
    }

    public final Object apply(Object obj) {
        return this.getMin.lambda$removeRecentBankAndSyncToUserConfig$13(this.length, (isRemoteExtension[]) obj);
    }
}
