package o;

import o.AppNode;

public class DocumentData$Justification extends setSpeed<AppNode.AnonymousClass2, installPath> {
    /* access modifiers changed from: protected */
    public installPath map(AppNode.AnonymousClass2 r8) {
        if (r8 != null) {
            return new installPath(r8.getText(), r8.isVisible(), r8.getVersion(), r8.getDate(), r8.getIconUrl());
        }
        return null;
    }
}
