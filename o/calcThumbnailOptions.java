package o;

import android.content.Context;

public final class calcThumbnailOptions extends decodeByteArray {
    public calcThumbnailOptions(Context context, setCancelOnTouchOutside$core setcancelontouchoutside_core, fillPageCommonInfo fillpagecommoninfo) {
        super(context, setcancelontouchoutside_core, fillpagecommoninfo);
    }

    /* access modifiers changed from: protected */
    public final String length() {
        if (setMax() == null) {
            return "";
        }
        return setMax().getRedirectUrl();
    }
}
