package o;

final class setRootView implements RedDotDrawable {
    private final getFrameMarginVertical$core getMax;
    private final registerWorker getMin;

    public setRootView(getFrameMarginVertical$core getframemarginvertical_core, registerWorker registerworker) {
        this.getMax = getframemarginvertical_core;
        this.getMin = registerworker;
    }

    public final Object apply(Object obj) {
        return this.getMax.lambda$saveBalance$18(this.getMin, (attach) obj);
    }
}
