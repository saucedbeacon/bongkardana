package o;

import io.reactivex.annotations.Nullable;
import o.TitleBarRightButtonView;

public abstract class encryptString<K, T> extends TitleBarRightButtonView.AnonymousClass1<T> {
    final K setMin;

    public encryptString(@Nullable K k) {
        this.setMin = k;
    }
}
