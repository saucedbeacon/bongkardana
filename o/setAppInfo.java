package o;

import android.widget.ImageView;
import id.dana.dialog.DialogWithImage;
import o.LandScapeTinyMenuPopupWindow;
import o.Popup;
import o.setOnMenuWindowDismissListener;

public final class setAppInfo implements Popup.getMax {
    private final DialogWithImage setMin;

    public setAppInfo(DialogWithImage dialogWithImage) {
        this.setMin = dialogWithImage;
    }

    public final void setMin(String str, ImageView imageView) {
        new LandScapeTinyMenuPopupWindow.AnonymousClass1().getMax(this.setMin.length, imageView, str, 0, (setOnMenuWindowDismissListener.length) null);
    }
}
