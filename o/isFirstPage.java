package o;

import org.json.JSONArray;

final class isFirstPage implements RedDotDrawable {
    public static final isFirstPage length = new isFirstPage();

    public final Object apply(Object obj) {
        return handleMessage.lambda$getCertificatesUrl$5((JSONArray) obj);
    }
}
