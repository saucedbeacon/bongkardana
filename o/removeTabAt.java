package o;

import android.graphics.Canvas;
import android.graphics.Matrix;
import android.graphics.RectF;

public interface removeTabAt extends animateToTab {
    void setMax(Canvas canvas, Matrix matrix, int i);

    void setMin(RectF rectF, Matrix matrix, boolean z);
}
