package o;

import android.graphics.Paint;
import androidx.annotation.NonNull;

public final class onScrolled extends offsetChildrenHorizontal {
    public onScrolled(@NonNull Paint paint, @NonNull onChildDetachedFromWindow onchilddetachedfromwindow) {
        super(paint, onchilddetachedfromwindow);
    }
}
