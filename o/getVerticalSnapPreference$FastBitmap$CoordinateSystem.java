package o;

import android.view.View;
import java.util.List;
import o.getVerticalSnapPreference;
import o.onTargetFound;

public class getVerticalSnapPreference$FastBitmap$CoordinateSystem extends getVerticalSnapPreference.getMin {
    private boolean setMax = false;

    public final void setMax() {
    }

    public final /* bridge */ /* synthetic */ void setMin(View view) {
        super.setMin(view);
    }

    public getVerticalSnapPreference$FastBitmap$CoordinateSystem(List<onTargetFound.length> list, String str, getVerticalSnapPreference.isInside isinside) {
        super(list, str, isinside, false);
    }

    public final void setMax(View view) {
        if (view != null && !this.setMax) {
            this.setMin.getMin(view, this.getMax, this.getMin);
        }
        this.setMax = view != null;
    }
}
