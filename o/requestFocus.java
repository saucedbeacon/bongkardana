package o;

import android.graphics.Canvas;
import android.graphics.ColorFilter;
import android.graphics.Matrix;
import androidx.annotation.Nullable;
import com.airbnb.lottie.LottieDrawable;
import com.airbnb.lottie.model.content.ShapeStroke;

public final class requestFocus extends setHoverListener {
    private final boolean getMin;
    private final String length;
    private final onKeyPreIme setMax;
    @Nullable
    private setInputType<ColorFilter, ColorFilter> toFloatRange;
    private final setInputType<Integer, Integer> toIntRange;

    public final void setMax(Canvas canvas, Matrix matrix, int i) {
        if (!this.getMin) {
            this.setMin.setColor(((getInputType) this.toIntRange).toIntRange());
            if (this.toFloatRange != null) {
                this.setMin.setColorFilter(this.toFloatRange.equals());
            }
            super.setMax(canvas, matrix, i);
        }
    }

    public final String setMin() {
        return this.length;
    }

    public final <T> void length(T t, @Nullable setTitleMarginBottom<T> settitlemarginbottom) {
        super.length(t, settitlemarginbottom);
        if (t == MenuPopupWindow$MenuDropDownListView.getMax) {
            this.toIntRange.getMin(settitlemarginbottom);
        } else if (t == MenuPopupWindow$MenuDropDownListView.asBinder) {
            setInputType<ColorFilter, ColorFilter> setinputtype = this.toFloatRange;
            if (setinputtype != null) {
                this.setMax.setMax.remove(setinputtype);
            }
            if (settitlemarginbottom == null) {
                this.toFloatRange = null;
                return;
            }
            isSubmitButtonEnabled issubmitbuttonenabled = new isSubmitButtonEnabled(settitlemarginbottom);
            this.toFloatRange = issubmitbuttonenabled;
            issubmitbuttonenabled.getMin.add(this);
            onKeyPreIme onkeypreime = this.setMax;
            setInputType<Integer, Integer> setinputtype2 = this.toIntRange;
            if (setinputtype2 != null) {
                onkeypreime.setMax.add(setinputtype2);
            }
        }
    }

    public requestFocus(LottieDrawable lottieDrawable, onKeyPreIme onkeypreime, ShapeStroke shapeStroke) {
        super(lottieDrawable, onkeypreime, shapeStroke.toIntRange.toPaintCap(), shapeStroke.IsOverlapping.toPaintJoin(), shapeStroke.equals, shapeStroke.getMax, shapeStroke.isInside, shapeStroke.setMax, shapeStroke.length);
        this.setMax = onkeypreime;
        this.length = shapeStroke.getMin;
        this.getMin = shapeStroke.getMin();
        getInputType getinputtype = new getInputType(shapeStroke.setMin.setMin);
        this.toIntRange = getinputtype;
        getinputtype.getMin.add(this);
        setInputType<Integer, Integer> setinputtype = this.toIntRange;
        if (setinputtype != null) {
            onkeypreime.setMax.add(setinputtype);
        }
    }
}
