package o;

import com.alibaba.ariver.engine.api.bridge.extension.BridgeCallback;

final class clearNeutralListeners implements RedDotManager {
    private final maxWidth getMin;
    private final BridgeCallback length;

    public clearNeutralListeners(maxWidth maxwidth, BridgeCallback bridgeCallback) {
        this.getMin = maxwidth;
        this.length = bridgeCallback;
    }

    public final void accept(Object obj) {
        this.getMin.lambda$getForexBalance$0(this.length, (String) obj);
    }
}
