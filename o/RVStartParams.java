package o;

import o.addAnimatorUpdateListener;

final class RVStartParams implements addAnimatorUpdateListener.getMax {
    private final Remote length;

    public RVStartParams(Remote remote) {
        this.length = remote;
    }

    public final Object processFacade(Object obj) {
        return ((canOverride) obj).queryCategorizedPromo(this.length);
    }
}
