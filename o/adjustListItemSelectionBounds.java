package o;

import java.util.HashMap;

public class adjustListItemSelectionBounds {
    public final HashMap<String, setGroupDividerEnabled> getMax = new HashMap<>();

    public final void getMin() {
        for (setGroupDividerEnabled length : this.getMax.values()) {
            length.length();
        }
        this.getMax.clear();
    }
}
