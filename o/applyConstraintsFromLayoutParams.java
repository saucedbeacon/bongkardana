package o;

import androidx.annotation.NonNull;
import com.bumptech.glide.load.EncodeStrategy;
import java.io.File;
import java.io.IOException;

public final class applyConstraintsFromLayoutParams implements setTranslationY<getViewWidget> {
    public final /* synthetic */ boolean setMax(@NonNull Object obj, @NonNull File file, @NonNull MotionHelper motionHelper) {
        return setMin((fireTrigger) obj, file);
    }

    @NonNull
    public final EncodeStrategy getMax(@NonNull MotionHelper motionHelper) {
        return EncodeStrategy.SOURCE;
    }

    private static boolean setMin(@NonNull fireTrigger<getViewWidget> firetrigger, @NonNull File file) {
        try {
            setInsetOffsetY.length(firetrigger.setMin().getMax.getMin.getMax.length().asReadOnlyBuffer(), file);
            return true;
        } catch (IOException unused) {
            return false;
        }
    }
}
