package o;

import o.setFirstHorizontalBias;

public abstract class setFirstHorizontalBias<CHILD extends setFirstHorizontalBias<CHILD, TranscodeType>, TranscodeType> implements Cloneable {
    resolveKeylineGravity<? super TranscodeType> getMax = getDesiredAnchoredChildRect.length();

    /* renamed from: length */
    public final CHILD clone() {
        try {
            return (setFirstHorizontalBias) super.clone();
        } catch (CloneNotSupportedException e) {
            throw new RuntimeException(e);
        }
    }
}
