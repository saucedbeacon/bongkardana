package o;

import javax.inject.Inject;
import o.TitleBarRightButtonView;

public class getBluetoothServices extends parseFailed<stopBleScan, Void> {
    private final onDescriptorWrite userRepository;

    @Inject
    public getBluetoothServices(appxLoadFailed appxloadfailed, getScheme getscheme, onDescriptorWrite ondescriptorwrite) {
        super(appxloadfailed, getscheme);
        this.userRepository = ondescriptorwrite;
    }

    /* access modifiers changed from: protected */
    public TitleBarRightButtonView.AnonymousClass1<stopBleScan> buildUseCaseObservable(Void voidR) {
        return this.userRepository.getBalance();
    }
}
