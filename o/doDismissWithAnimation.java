package o;

import android.content.Context;
import android.text.TextUtils;
import android.widget.ImageView;
import o.setOnMenuWindowDismissListener;

public class doDismissWithAnimation {
    /* access modifiers changed from: protected */
    public setTinyAppDesc setMax(String str, setOnMenuWindowDismissListener.length length) {
        if (RewriteQueriesToDropUnusedColumns.setMax(str)) {
            return new setOnShowListener(str, length);
        }
        return new initWindowManagerLayoutParams(str, length);
    }

    public final void getMax(Context context, ImageView imageView, String str, int i, setOnMenuWindowDismissListener.length length) {
        setTinyAppDesc settinyappdesc;
        if (!TextUtils.isEmpty(str)) {
            settinyappdesc = setMax(str, length);
        } else {
            int max = dispatchOnCancelled.setMax(i);
            if (i != max) {
                onCanceled oncanceled = new onCanceled(i, max, 1);
                onCancelLoad.setMax(1925317054, oncanceled);
                onCancelLoad.getMin(1925317054, oncanceled);
            }
            settinyappdesc = new LandScapeTinyMenuPopupWindow(i, length);
        }
        settinyappdesc.length(context, imageView);
    }
}
