package o;

abstract class setFooterView<T, R> extends setBadgeData<R> {
    protected final setBadgeData<T> setMin;

    setFooterView(setBadgeData<T> setbadgedata) {
        this.setMin = (setBadgeData) setRefreshAnimation.getMax(setbadgedata, "source is null");
    }
}
