package o;

import android.graphics.Bitmap;
import androidx.annotation.NonNull;
import java.security.MessageDigest;

public final class setDpMargin extends setType {
    private static final byte[] setMax = "com.bumptech.glide.load.resource.bitmap.CircleCrop.1".getBytes(setMin);

    /* access modifiers changed from: protected */
    public final Bitmap setMax(@NonNull rebuildScene rebuildscene, @NonNull Bitmap bitmap, int i, int i2) {
        return setTag.setMin(rebuildscene, bitmap, i, i2);
    }

    public final boolean equals(Object obj) {
        return obj instanceof setDpMargin;
    }

    public final int hashCode() {
        return "com.bumptech.glide.load.resource.bitmap.CircleCrop.1".hashCode();
    }

    public final void setMax(@NonNull MessageDigest messageDigest) {
        messageDigest.update(setMax);
    }
}
