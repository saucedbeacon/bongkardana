package o;

import android.content.Context;
import android.os.Build;
import androidx.annotation.NonNull;
import androidx.work.NetworkType;

public final class onReceiveContent extends setImageDrawable<onTextContextMenuItem> {
    private static final String setMin = setSupportBackgroundTintList.length("NetworkNotRoamingCtrlr");

    /* access modifiers changed from: package-private */
    public final /* synthetic */ boolean getMin(@NonNull Object obj) {
        return length((onTextContextMenuItem) obj);
    }

    public onReceiveContent(Context context, getMinWidthMinor getminwidthminor) {
        super(AppCompatPopupWindow.setMax(context, getminwidthminor).getMin);
    }

    /* access modifiers changed from: package-private */
    public final boolean getMax(@NonNull getInternalPopup getinternalpopup) {
        return getinternalpopup.toFloatRange.length == NetworkType.NOT_ROAMING;
    }

    private static boolean length(@NonNull onTextContextMenuItem ontextcontextmenuitem) {
        if (Build.VERSION.SDK_INT >= 24) {
            return !ontextcontextmenuitem.getMax() || !ontextcontextmenuitem.length();
        }
        setSupportBackgroundTintList.getMax();
        return !ontextcontextmenuitem.getMax();
    }
}
