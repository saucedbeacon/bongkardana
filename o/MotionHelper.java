package o;

import androidx.annotation.NonNull;
import java.security.MessageDigest;

public final class MotionHelper implements setPivotY {
    public final PlaybackStateCompat<setTranslationX<?>, Object> length = new onChildViewsChanged();

    public final boolean equals(Object obj) {
        if (obj instanceof MotionHelper) {
            return this.length.equals(((MotionHelper) obj).length);
        }
        return false;
    }

    public final int hashCode() {
        return this.length.hashCode();
    }

    public final void setMax(@NonNull MessageDigest messageDigest) {
        for (int i = 0; i < this.length.size(); i++) {
            int i2 = i << 1;
            setTranslationX settranslationx = (setTranslationX) this.length.isInside[i2];
            settranslationx.length.length(settranslationx.setMax(), this.length.isInside[i2 + 1], messageDigest);
        }
    }

    public final String toString() {
        StringBuilder sb = new StringBuilder("Options{values=");
        sb.append(this.length);
        sb.append('}');
        return sb.toString();
    }
}
