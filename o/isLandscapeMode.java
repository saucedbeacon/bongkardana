package o;

import androidx.annotation.Nullable;
import com.airbnb.lottie.LottieDrawable;

public interface isLandscapeMode {
    @Nullable
    animateToTab length(LottieDrawable lottieDrawable, onKeyPreIme onkeypreime);
}
