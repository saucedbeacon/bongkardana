package o;

import o.getAutoLogActivities;
import org.yaml.snakeyaml.events.Event;
import org.yaml.snakeyaml.tokens.Token;

class getAutoLogActivities$Grayscale$Algorithm implements getUploader {
    final /* synthetic */ getAutoLogActivities getMin;

    private getAutoLogActivities$Grayscale$Algorithm(getAutoLogActivities getautologactivities) {
        this.getMin = getautologactivities;
    }

    /* synthetic */ getAutoLogActivities$Grayscale$Algorithm(getAutoLogActivities getautologactivities, byte b) {
        this(getautologactivities);
    }

    public final Event setMax() {
        if (this.getMin.getMax.getMax(Token.ID.Value)) {
            Token min = this.getMin.getMax.getMin();
            if (!this.getMin.getMax.getMax(Token.ID.FlowEntry, Token.ID.FlowSequenceEnd)) {
                AbtestInfoGetter length = this.getMin.setMin;
                length.setMax.add(new getAutoLogActivities.invokeSuspend(this.getMin, (byte) 0));
                return this.getMin.getMax(false, false);
            }
            getAutoLogActivities getautologactivities = this.getMin;
            getUploader unused = getautologactivities.isInside = new getAutoLogActivities.invokeSuspend(getautologactivities, (byte) 0);
            return getAutoLogActivities.setMin(min.toFloatRange);
        }
        getAutoLogActivities getautologactivities2 = this.getMin;
        getUploader unused2 = getautologactivities2.isInside = new getAutoLogActivities.invokeSuspend(getautologactivities2, (byte) 0);
        return getAutoLogActivities.setMin(this.getMin.getMax.length().getMin);
    }
}
