package o;

import o.onNext;

final class onTrigger implements RedDotManager {
    private final onNext.length getMax;

    public onTrigger(onNext.length length) {
        this.getMax = length;
    }

    public final void accept(Object obj) {
        this.getMax.getMax((Throwable) obj);
    }
}
