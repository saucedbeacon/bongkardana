package o;

import android.graphics.Bitmap;
import android.os.ParcelFileDescriptor;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RequiresApi;
import java.io.IOException;
import o.setIds;

@RequiresApi(21)
public final class applyLayoutFeatures implements setElevation<ParcelFileDescriptor, Bitmap> {
    private final setAllowsGoneWidget getMin;

    @Nullable
    public final /* synthetic */ fireTrigger getMax(@NonNull Object obj, int i, int i2, @NonNull MotionHelper motionHelper) throws IOException {
        setAllowsGoneWidget setallowsgonewidget = this.getMin;
        return setallowsgonewidget.setMax(new setIds.getMax((ParcelFileDescriptor) obj, setallowsgonewidget.IsOverlapping, setallowsgonewidget.toIntRange), i, i2, motionHelper, setAllowsGoneWidget.equals);
    }

    public final /* synthetic */ boolean length(@NonNull Object obj, @NonNull MotionHelper motionHelper) throws IOException {
        return getMin();
    }

    public applyLayoutFeatures(setAllowsGoneWidget setallowsgonewidget) {
        this.getMin = setallowsgonewidget;
    }

    private static boolean getMin() {
        return setAllowsGoneWidget.setMax();
    }
}
