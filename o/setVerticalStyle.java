package o;

import android.content.Context;
import androidx.annotation.NonNull;
import com.bumptech.glide.Glide;
import o.getContent;

public final class setVerticalStyle implements getContent.getMax {
    @NonNull
    public final setHorizontalGap length(@NonNull Glide glide, @NonNull Group group, @NonNull getEmptyVisibility getemptyvisibility, @NonNull Context context) {
        return new updateCornerMarking(glide, group, getemptyvisibility, context);
    }
}
