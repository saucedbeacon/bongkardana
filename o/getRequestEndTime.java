package o;

import o.addAnimatorUpdateListener;

final class getRequestEndTime implements addAnimatorUpdateListener.getMax {
    private final getInstallTime setMin;

    public getRequestEndTime(getInstallTime getinstalltime) {
        this.setMin = getinstalltime;
    }

    public final Object processFacade(Object obj) {
        return ((setRequestEndTime) obj).getVerifyToken(this.setMin);
    }
}
