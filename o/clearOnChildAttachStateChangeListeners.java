package o;

import android.graphics.Rect;
import android.hardware.camera2.CameraCharacteristics;
import android.hardware.camera2.params.MeteringRectangle;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RequiresApi;

@RequiresApi(21)
public abstract class clearOnChildAttachStateChangeListeners extends setScrollingTouchSlop {
    private boolean setMax = true;

    /* access modifiers changed from: protected */
    public abstract void getMin(@NonNull getFullClassName getfullclassname, @Nullable MeteringRectangle meteringRectangle);

    protected clearOnChildAttachStateChangeListeners() {
    }

    public final void length(@NonNull getFullClassName getfullclassname) {
        super.length(getfullclassname);
        getMin(getfullclassname, this.setMax ? new MeteringRectangle((Rect) setMin(CameraCharacteristics.SENSOR_INFO_ACTIVE_ARRAY_SIZE, new Rect()), 0) : null);
    }
}
