package o;

import android.content.Context;

public final class getTypefaceByCat {
    public setMin setMax;

    public /* synthetic */ getTypefaceByCat(setMin setmin, byte b) {
        this(setmin);
    }

    private getTypefaceByCat(setMin setmin) {
        this.setMax = setmin;
    }

    public final boolean length() {
        return this.setMax.equals;
    }

    public static class setMin {
        public String IsOverlapping;
        public boolean equals;
        public Context getMax;
        public String getMin;
        public String length;
        public String setMax;
        public String setMin;
        public boolean toIntRange;

        public final setMin setMin(String str) {
            int length2 = str != null ? str.length() : 0;
            int max = dispatchOnCancelled.setMax(length2);
            if (length2 != max) {
                onCanceled oncanceled = new onCanceled(length2, max, 1);
                onCancelLoad.setMax(-991517672, oncanceled);
                onCancelLoad.getMin(-991517672, oncanceled);
            }
            this.IsOverlapping = str;
            return this;
        }
    }
}
