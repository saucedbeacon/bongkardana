package o;

import java.util.Map;
import o.CircleFrameLayout;

final class BisBehavTask extends clearLogs {
    private getLogs getMax;
    private Map<String, ClientConfigContentPB> length;

    BisBehavTask(Map<String, ClientConfigContentPB> map, getLogs getlogs) {
        this.length = map;
        this.getMax = getlogs;
    }

    public final void setMax(LogFactory logFactory, getCricleColor getcriclecolor) {
        setMin(logFactory, getcriclecolor);
        super.setMax(logFactory, getcriclecolor);
    }

    public final void getMin(LogFactory logFactory, getCricleColor getcriclecolor) {
        setMin(logFactory, getcriclecolor);
        super.getMin(logFactory, getcriclecolor);
    }

    private void setMin(LogFactory logFactory, getCricleColor getcriclecolor) {
        ClientConfigContentPB clientConfigContentPB;
        if (getcriclecolor.length() == 110) {
            LogFactory min = logFactory.setMin();
            int i = 0;
            LogFactory min2 = min.getMin();
            while (min2 != null) {
                min2 = min2.getMin();
                i++;
            }
            if (i == 0) {
                ((ClientConfigContentPB) getcriclecolor.ICustomTabsCallback$Stub$Proxy).setMin = true;
            }
            if (this.length != null) {
                String str = null;
                if (min.length() == 39) {
                    str = ((CircleFrameLayout.WidgetListener) min).equals;
                } else if (min.length() == 33) {
                    str = ((CircleFrameLayout.WidgetListener) min.setMin().getMin()).equals;
                } else if (min.length() == 34) {
                    throw getStyleId.setMin();
                }
                if (str != null && (clientConfigContentPB = this.length.get(str)) != null && i == clientConfigContentPB.length.onTransact && !clientConfigContentPB.length.Grayscale$Algorithm() && i <= 32) {
                    logFactory.setMax(9, (Object) clientConfigContentPB);
                    if (!clientConfigContentPB.setMax()) {
                        int size = this.getMax.size();
                        this.getMax.add(clientConfigContentPB);
                        if (size < 0 || clientConfigContentPB.getMax >= 0) {
                            getStyleId.setMin();
                        }
                        clientConfigContentPB.getMax = size;
                    }
                }
            }
        }
    }
}
