package o;

import android.location.Location;
import java.util.Map;

final class EngineRouter implements RedDotManager {
    private final setTargetExtension getMin;
    private final String setMin;

    final class RenderInitListener implements removeNode {
        private final boolean getMax;
        private final generateUniqueId getMin;
        private final Location length;
        private final setTargetExtension setMin;

        public RenderInitListener(setTargetExtension settargetextension, boolean z, Location location, generateUniqueId generateuniqueid) {
            this.setMin = settargetextension;
            this.getMax = z;
            this.length = location;
            this.getMin = generateuniqueid;
        }

        public final Object setMax(Object obj, Object obj2) {
            return this.setMin.lambda$getNearbyShopByRadius$6(this.getMax, this.length, this.getMin, (Map) obj, (Map) obj2);
        }
    }

    public EngineRouter(setTargetExtension settargetextension, String str) {
        this.getMin = settargetextension;
        this.setMin = str;
    }

    public final void accept(Object obj) {
        this.getMin.lambda$getNearbyShops$3(this.setMin, (H5FileUtil) obj);
    }
}
