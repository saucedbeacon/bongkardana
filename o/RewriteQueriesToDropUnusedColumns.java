package o;

import android.content.Context;
import android.graphics.Bitmap;
import android.graphics.drawable.PictureDrawable;
import android.net.Uri;
import android.text.TextUtils;
import android.widget.ImageView;
import androidx.annotation.DrawableRes;
import com.bumptech.glide.Glide;
import java.util.List;

public final class RewriteQueriesToDropUnusedColumns {
    private updatePostLayout<?> equals;
    private Context getMax;
    private ImageView getMin;
    private dispatchApplyWindowInsetsToBehaviors length;
    private String setMax;
    @DrawableRes
    private int setMin;
    private List<parseBehavior<PictureDrawable>> toIntRange;

    public /* synthetic */ RewriteQueriesToDropUnusedColumns(getMax getmax, byte b) {
        this(getmax);
    }

    private RewriteQueriesToDropUnusedColumns(getMax getmax) {
        this.getMax = getmax.getMax;
        this.setMax = getmax.getMin;
        this.setMin = getmax.setMax;
        this.getMin = getmax.setMin;
        this.length = getmax.length;
        this.equals = getmax.isInside;
        this.toIntRange = getmax.equals;
        getMin();
    }

    public static boolean setMax(String str) {
        return str != null && str.contains(".svg");
    }

    private void getMin() {
        if (this.getMax != null && !TextUtils.isEmpty(this.setMax) && this.getMin != null) {
            updateMessageMenuItem<ResourceType> min = ((updateCornerMarking) Glide.getMax(this.getMax)).setMax(PictureDrawable.class).setMax(evaluate.setMin).getMax(new getBackingFieldMap()).getMin(Uri.parse(this.setMax));
            int i = this.setMin;
            if (i != 0) {
                min.length(i).getMin(this.setMin);
            }
            dispatchApplyWindowInsetsToBehaviors dispatchapplywindowinsetstobehaviors = this.length;
            if (dispatchapplywindowinsetstobehaviors != null) {
                min.getMin((getTopSortedChildren<?>) dispatchapplywindowinsetstobehaviors);
            }
            updatePostLayout<?> updatepostlayout = this.equals;
            if (updatepostlayout != null) {
                min.setMax((updatePostLayout<Bitmap>) updatepostlayout);
            }
            List<parseBehavior<PictureDrawable>> list = this.toIntRange;
            if (list != null && !list.isEmpty()) {
                for (parseBehavior<PictureDrawable> min2 : this.toIntRange) {
                    min.setMin((parseBehavior<ResourceType>) min2);
                }
            }
            min.length(this.getMin);
        }
    }

    public static class getMax {
        public List<parseBehavior<PictureDrawable>> equals;
        Context getMax;
        public String getMin;
        public updatePostLayout<?> isInside;
        public dispatchApplyWindowInsetsToBehaviors length;
        @DrawableRes
        public int setMax;
        public ImageView setMin;

        public /* synthetic */ getMax(Context context, byte b) {
            this(context);
        }

        private getMax(Context context) {
            this.getMax = context;
        }
    }
}
